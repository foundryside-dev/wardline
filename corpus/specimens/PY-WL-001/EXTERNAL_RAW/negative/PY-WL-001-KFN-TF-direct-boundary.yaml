---
specimen_id: "PY-WL-001-KFN-TF-direct-boundary"
description: "SUPPRESS at EXTERNAL_RAW: Taint-flow: external data with .get default reaching audit-tier consumer without validation"
rule: "PY-WL-001"
category: "taint_flow"
fragment: |
  def fetch_external(api_client):
      """Tier 4 boundary: fetches raw external data."""
      response = api_client.get("/data")
      return response.json()

  def consume_in_audit_context(api_client):
      """Audit-tier consumer uses .get with default on external data."""
      payload = fetch_external(api_client)
      user_id = payload.get("user_id", "anonymous")
      return user_id
taint_state: "EXTERNAL_RAW"
expected_rules: []
expected_severity: null
expected_exceptionability: null
expected_match: false
sha256: "d0084426e431f159bbee0f4435aec2e6c61c287fd27dbb1bd863030c312d676e"
verdict: "known_false_negative"
