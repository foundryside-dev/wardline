---
specimen_id: "PY-WL-001-TP-AFN-nested-get"
description: "Adversarial false negative: dict.get with default hidden inside chained method calls"
rule: "PY-WL-001"
category: "adversarial_false_negative"
fragment: |
  def extract_config(raw_input):
      """Default hidden in chained call — scanner must still detect .get(k, default)."""
      config = raw_input.get("settings", {}).get("timeout", 30)
      return config
taint_state: "EXTERNAL_RAW"
expected_rules:
- PY-WL-001
expected_severity: ERROR
expected_exceptionability: STANDARD
expected_match:
  line: 3
  text: "config = raw_input.get(\"settings\", {}).get(\"timeout\", 30)"
  function: extract_config
sha256: "6183b014e422cb968f015b62aa3e3fd07b7ed302eba6a781b6e3f562ea115ca5"
verdict: "true_positive"
expected_match_source: ast-reimplemented
