requirements:
  - id: WL-FIT-PY-001
    title: Decorator discovery is AST-based
    statement: >
      The Python Wardline-Core implementation MUST discover wardline decorators
      from the target codebase's AST and SHALL NOT rely on runtime introspection
      or dynamic attribute inspection as its primary discovery mechanism.
    normative_sources:
      - file: docs/spec/wardline-02-A-python-binding.md
        section: "A.3 Interface contract (Wardline-Core)"
    verification:
      method: static_review_and_unit_tests
      target_paths:
        - src/wardline/scanner/discovery.py
        - src/wardline/scanner/context.py
        - tests/unit/test_wp04_hardening.py
    fitness_dimensions: [correctness, interoperability]
    notes: Runtime metadata helpers may exist, but they are not the normative discovery path.

  - id: WL-FIT-PY-002
    title: "`schema_default()` is recognised as the governed default marker"
    statement: >
      The Python implementation MUST recognise `schema_default()` as the
      PY-WL-001 suppression marker and only treat it as governed when the
      overlay declaration supports it.
    normative_sources:
      - file: docs/spec/wardline-02-A-python-binding.md
        section: "A.3 Interface contract (Wardline-Core)"
    verification:
      method: static_review_and_unit_tests
      target_paths:
        - src/wardline/decorators/schema.py
        - src/wardline/scanner/rules/py_wl_001.py
        - tests/unit/scanner/test_py_wl_001.py
    fitness_dimensions: [correctness, conformance]
    notes: This is a Python-specific part of the normative Wardline-Core interface contract.

  - id: WL-FIT-PY-003
    title: Mandatory result-level SARIF properties are emitted
    statement: >
      Each finding emitted by the Python Wardline-Core implementation MUST carry
      the mandatory result-level property bag fields required by the Python
      interface contract.
    normative_sources:
      - file: docs/spec/wardline-02-A-python-binding.md
        section: "A.3 Interface contract (Wardline-Core)"
    verification:
      method: unit_tests
      target_paths:
        - src/wardline/scanner/sarif.py
        - tests/unit/scanner/test_sarif.py
    fitness_dimensions: [correctness, interoperability]
    notes: This requirement is satisfied by the specific A.3 result keys, not by the full Part I result bag.

  - id: WL-FIT-PY-004
    title: Decorator composition semantics are honoured
    statement: >
      The Python implementation MUST honour the binding’s normative decorator
      composition semantics, including the rule that `@int_data` alone produces
      `UNKNOWN_RAW` and only composition with `@restoration_boundary` can
      restore a higher effective tier according to the Part I evidence matrix.
    normative_sources:
      - file: docs/spec/wardline-02-A-python-binding.md
        section: "A.3 Interface contract (Wardline-Core)"
    informative_sources:
      - file: docs/spec/wardline-02-A-python-binding.md
        section: "A.4.2 Decorator mapping table"
      - file: docs/spec/wardline-02-A-python-binding.md
        section: "A.4.3 Non-obvious design rationale"
    verification:
      method: runtime_and_scanner_tests
      target_paths:
        - src/wardline/decorators
        - src/wardline/core/registry.py
        - tests/unit/decorators
        - tests/unit/scanner/test_py_wl_007.py
    fitness_dimensions: [correctness, conformance]
    notes: This is the binding-side bridge between Part I restoration semantics and Python syntax.

  - id: WL-FIT-PY-005
    title: Unresolvable third-party delegation is handled conservatively
    statement: >
      For PY-WL-008 / framework WL-007 delegation analysis, when third-party
      source is unavailable or delegation resolution is not implemented, the
      Python implementation MUST treat the delegation as unresolvable for
      minimum-conformance purposes and evaluate the boundary conservatively.
    normative_sources:
      - file: docs/spec/wardline-02-A-python-binding.md
        section: "A.3 Interface contract (Wardline-Core)"
    verification:
      method: rule_tests
      target_paths:
        - src/wardline/scanner/rejection_path.py
        - tests/unit/scanner/test_delegated_rejection.py
        - tests/unit/scanner/test_rejection_path_convergence.py
    fitness_dimensions: [correctness, robustness]
    notes: Optional deeper resolution is a SHOULD; conservative fallback is the MUST.

  - id: WL-FIT-PY-006
    title: Implemented rules and binding corpus are declared
    statement: >
      The Python implementation MUST declare which rules it implements and MUST
      maintain golden corpus specimens for those rules.
    normative_sources:
      - file: docs/spec/wardline-02-A-python-binding.md
        section: "A.3 Interface contract (Wardline-Core)"
    verification:
      method: rule_registry_and_corpus_review
      target_paths:
        - src/wardline/scanner/rules/__init__.py
        - src/wardline/scanner/sarif.py
        - corpus/specimens
        - tests/unit/scanner/test_rules.py
    fitness_dimensions: [conformance, interoperability]
    notes: >
      The complete Wardline-Core tool implements PY-WL-001 through PY-WL-010
      (10 rules). The binding severity matrix is 80 cells (10 rules x 8 taint
      states). PY-WL-001 through PY-WL-005 are syntactic patterns detectable
      by per-file AST analysis (the advisory ruff path). PY-WL-006 through
      PY-WL-010 require semantic context (audit-path awareness, tier
      classification, structural verification, validation ordering, restoration
      symmetry) and are implemented by the reference scanner only. This
      advisory/authoritative split is normative in §A.3. This overlaps with
      scanner conformance, but remains part of the Python interface contract.

  - id: WL-FIT-PY-007
    title: Verification mode exists and is deterministic
    statement: >
      The Python implementation MUST support `--verification-mode` and MUST
      produce deterministic byte-identical SARIF for identical input in that
      mode.
    normative_sources:
      - file: docs/spec/wardline-02-A-python-binding.md
        section: "A.3 Interface contract (Wardline-Core)"
      - file: docs/spec/wardline-01-10-verification-properties.md
        section: "10. Verification properties"
    verification:
      method: integration_tests
      target_paths:
        - src/wardline/cli/scan.py
        - tests/integration/test_determinism.py
        - tests/integration/test_self_hosting.py
    fitness_dimensions: [conformance, assessability]
    notes: This requirement is about repeatability, not completeness of the property bag.

  - id: WL-FIT-PY-008
    title: Mandatory run-level SARIF properties are emitted
    statement: >
      The Python implementation MUST emit the required run-level SARIF
      properties from the Python interface contract, including manifest and
      input identity properties needed for independent determinism checking.
    normative_sources:
      - file: docs/spec/wardline-02-A-python-binding.md
        section: "A.3 Interface contract (Wardline-Core)"
      - file: docs/spec/wardline-01-10-verification-properties.md
        section: "10.1 Findings interchange format"
    verification:
      method: rule_registry_and_sarif_review
      target_paths:
        - src/wardline/cli/scan.py
        - src/wardline/scanner/sarif.py
        - tests/unit/scanner/test_sarif.py
        - tests/integration/test_scan_cmd.py
    fitness_dimensions: [interoperability, conformance]
    notes: Missing `wardline.inputHash` and related run properties block independent assessment.

  - id: WL-FIT-PY-009
    title: Manifest is consumed and validated before findings
    statement: >
      The Python Wardline-Core tool MUST consume the wardline manifest
      (wardline.yaml and any overlays) and validate the manifest against the
      framework's JSON Schemas before producing findings. A tool that produces
      findings without validating the manifest is non-conformant.
    normative_sources:
      - file: docs/spec/wardline-02-A-python-binding.md
        section: "A.3 Interface contract (Wardline-Core)"
    verification:
      method: integration_tests
      target_paths:
        - src/wardline/cli/scan.py
        - src/wardline/manifest/loader.py
        - src/wardline/manifest/schemas/wardline.schema.json
        - tests/integration/test_scan_cmd.py
    fitness_dimensions: [conformance, correctness]
    notes: >
      This is §A.3 item 1. The scanner must validate the manifest before
      producing findings, not merely load it. Distinct from MAN-001 which
      covers the loader; this covers the scanner's obligation to call
      validation before emitting results.

  - id: WL-FIT-PY-010
    title: Contradictory decorator combinations are detected
    statement: >
      The scanner MUST detect mutually exclusive decorator combinations as
      ERROR findings (SCN-021). The Python binding defines 29 detected
      combinations (26 contradictory, 3 suspicious) including tier conflicts
      (e.g., @integral_read with @validates_shape), semantic conflicts
      (e.g., @fail_closed with @fail_open), and determinism conflicts
      (e.g., @deterministic with @time_dependent).
    normative_sources:
      - file: docs/spec/wardline-02-A-python-binding.md
        section: "A.3 Interface contract (Wardline-Core)"
    informative_sources:
      - file: docs/spec/wardline-02-A-python-binding.md
        section: "A.4.3 Non-obvious design rationale"
    verification:
      method: unit_tests
      target_paths:
        - src/wardline/scanner/discovery.py
        - src/wardline/scanner/rules
        - tests/unit/scanner
    fitness_dimensions: [correctness, conformance]
    notes: >
      The 29 pairs are enumerated in the binding spec. Verify the scanner
      implements and tests detection for each pair.

  - id: WL-FIT-PY-011
    title: Error handling and exit codes follow binding contract
    statement: >
      The Python scanner MUST follow the binding's error handling table:
      invalid wardline.yaml exits non-zero without scanning, missing
      wardline.yaml exits non-zero without scanning, syntax errors in
      Tier 1 modules escalate from WARNING to ERROR, and deleted baseline
      files produce ERROR (not silent re-establishment). Exit codes MUST
      be: 0 (no ERROR findings), 1 (at least one ERROR), 2 (internal error),
      3 (direct law — regime cannot produce meaningful enforcement output;
      `wardline regime` only).
    normative_sources:
      - file: docs/spec/wardline-02-A-python-binding.md
        section: "A.10 Error handling and control law"
    verification:
      method: integration_tests
      target_paths:
        - src/wardline/cli/scan.py
        - tests/integration/test_scan_cmd.py
    fitness_dimensions: [correctness, conformance]
    notes: >
      Exit code semantics are normative. Verify that each error condition
      maps to the correct exit code and escalation behavior.

  - id: WL-FIT-PY-012
    title: Analysis level is emitted per finding
    statement: >
      Each SARIF result MUST carry wardline.analysisLevel (integer) in its
      property bag, reflecting the minimum analysis level whose capabilities
      are sufficient to justify the finding. The tool MUST emit the lowest
      level whose capabilities are sufficient to produce the finding.
    normative_sources:
      - file: docs/spec/wardline-02-A-python-binding.md
        section: "A.3 Interface contract (Wardline-Core)"
    verification:
      method: unit_tests
      target_paths:
        - src/wardline/scanner/sarif.py
        - tests/unit/scanner/test_sarif.py
    fitness_dimensions: [interoperability, conformance]
    notes: >
      Analysis levels enable assessors to understand which scanner
      capabilities were needed for each finding.

  - id: WL-FIT-PY-013
    title: "PY-WL-010: Restoration symmetry for serialization boundaries"
    statement: >
      The Python implementation MUST implement PY-WL-010, which fires when a
      function declared @integral_read or @integral_construction (Group 1) has
      a data source that is a manifest-declared serialization boundary
      (BoundaryEntry with serialization_boundary: true), and the function does
      not co-declare @restoration_boundary (Group 17) with at least one
      evidence category, and inputs do not trace through a restoration boundary
      within two-hop scope. Severity is ERROR/UNCONDITIONAL across all 8
      taint states. PY-WL-010 maps to framework WL-009.
    normative_sources:
      - file: docs/spec/wardline-02-A-python-binding.md
        section: "A.4.4 PY-WL-010: Tier 1 promotion on serialisation path without restoration evidence"
    verification:
      method: rule_tests
      target_paths:
        - src/wardline/scanner/rules/py_wl_010.py
        - tests/unit/scanner/test_py_wl_010.py
        - corpus/specimens/PY-WL-010
    fitness_dimensions: [correctness, conformance]
    notes: >
      PY-WL-010 is a structural verification rule. All three conditions
      (Group 1 decorator, serialization boundary source, missing restoration
      boundary) must hold simultaneously. Two-hop scope limits the
      restoration-boundary search to the function and its immediate callees.

  - id: WL-FIT-PY-014
    title: "SUP-010: Frozen dataclass mutable container detection"
    statement: >
      The Python implementation MUST implement SUP-010, which detects frozen
      dataclasses with mutable container fields (dict, list, set, Mapping,
      MutableMapping) that lack a deep-freeze call in __post_init__. SUP-010
      is a binding-specific supplementary rule with no framework counterpart,
      implementing the non-normative deep-immutability principle from Part I
      section 8. The mutable container type list MUST be extensible via
      `mutable_container_types` in scanner configuration, and the
      recognised deep-freeze function list MUST be extensible via
      `deep_freeze_functions` in scanner configuration. Severity matrix: INTEGRAL=E/U, ASSURED=E/St,
      GUARDED=W/R, EXT_RAW=Su/T, UNK_RAW=Su/T, UNK_GUARDED=W/R,
      UNK_ASSURED=E/St, MIXED_RAW=Su/T.
    normative_sources:
      - file: docs/spec/wardline-02-A-python-binding.md
        section: "A.4.5 Supplementary rules: SUP-010 and SUP-011"
    verification:
      method: rule_tests
      target_paths:
        - src/wardline/scanner/rules/sup_010.py
        - tests/unit/scanner/test_sup_010.py
        - corpus/specimens/SUP-010
    fitness_dimensions: [correctness, conformance]
    notes: >
      SUP-010 uses AST visitor analysis on frozen dataclasses. Detection
      checks for mutable field types and verifies __post_init__ calls a
      recognised deep-freeze function with `self.<field>` as argument for
      each mutable field — the check is per-field, not just presence of
      any deep-freeze call. No framework WL-xxx counterpart exists.

  - id: WL-FIT-PY-015
    title: "SUP-011: Conditional freeze guard detection"
    statement: >
      The Python implementation MUST implement SUP-011, which detects
      isinstance type guards in __post_init__ of a frozen dataclass that
      conditionally skip freezing. Detection matches isinstance calls
      where the first argument is `self.<field>`, any checked type is in
      {dict, tuple, MappingProxyType, frozenset, Mapping, list, set},
      and the call is the test of an `if` whose body contains
      `object.__setattr__` calls. SUP-011 fires independently of SUP-010
      and is a binding-specific supplementary rule with no framework
      counterpart.
      Severity matrix: INTEGRAL=E/U, ASSURED=E/St, GUARDED=W/R,
      EXT_RAW=Su/T, UNK_RAW=Su/T, UNK_GUARDED=W/R, UNK_ASSURED=E/St,
      MIXED_RAW=Su/T.
    normative_sources:
      - file: docs/spec/wardline-02-A-python-binding.md
        section: "A.4.5 Supplementary rules: SUP-010 and SUP-011"
    verification:
      method: rule_tests
      target_paths:
        - src/wardline/scanner/rules/sup_011.py
        - tests/unit/scanner/test_sup_011.py
        - corpus/specimens/SUP-011
    fitness_dimensions: [correctness, conformance]
    notes: >
      SUP-011 fires independently of SUP-010. A conditional freeze guard
      (isinstance check that skips freezing for certain types) undermines
      the deep-immutability guarantee even when SUP-010 is satisfied.

  - id: WL-FIT-PY-016
    title: "PY-WL-002 deviation is a split-rule widening under section 7.1"
    statement: >
      The Python implementation MUST treat the PY-WL-002 severity deviation
      as a WIDENING authorized by the section 7.1 split-rule provision, not a
      narrowing. PY-WL-002 establishes its own matrix row because the
      language-specific falsy-substitution risk of `obj.attr or default` —
      silently replacing present-but-falsy attribute values (0, "", False,
      None) with the default — is absent from the framework-level WL-001
      dict-key access pattern. PY-WL-002 at EXTERNAL_RAW and UNKNOWN_RAW
      MUST be WARNING/RELAXED, and at MIXED_RAW MUST be WARNING/STANDARD.
    normative_sources:
      - file: docs/spec/wardline-02-A-python-binding.md
        section: "A.4.3 Non-obvious design rationale"
    informative_sources:
      - file: docs/spec/wardline-01-07-severity-matrix.md
        section: "7.1 Split-rule provision"
    verification:
      method: unit_tests
      target_paths:
        - src/wardline/scanner/rules/py_wl_002.py
        - tests/unit/scanner/test_py_wl_002.py
        - src/wardline/core/severity.py
    fitness_dimensions: [conformance, correctness]
    notes: >
      v0.3.0 clarifies that PY-WL-002 is a widening (not narrowing) of WL-001.
      MIXED_RAW severity is WARNING/STANDARD (was WARNING/RELAXED pre-v0.3.0).
