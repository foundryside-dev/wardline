# Plan rev 3.7 / spec rev 9 — go/no-go (review round 3)

`Date: 2026-08-11` · Plan: **GO-WITH-CONDITIONS** · Spec: **GO-WITH-CONDITIONS** · **0 blocking**
· 31 candidates, 26 survived refutation, 5 killed · 13 agents, 0 errors

**Blocking trend across rounds: 11 → 6 → 1 → 0.**

## Headline

Blocking count this round: 0. Trend 11 → 6 → 1 → 0. Both artifacts are GO-WITH-CONDITIONS against a closed list of 15. I weighed two items against NO-GO and neither crossed my pre-committed bar: (a) the two sanctioned MCP-golden re-freezes are unexecutable as written — I reproduced the failure — but the repair sits forty lines above the helper the script already imports, in the same module, so it is an execution stop WITH an in-scope repair; (b) S0 close condition (iv) names a QE measurement — "the same four frozen-oracle suites", the two-symbol over-approximation probe, the blinded negative control — that a whole-repo grep shows is defined nowhere, but it gates a close receipt rather than the build, the property it guards is independently enforced by Global Constraints :35's no-regeneration list plus green-suite-after-every-task, and the gap is enumerable and closable. The spec-pin cluster, which two of seven derivations called BLOCKING, is MAJOR: I computed the blob myself (159,200 bytes, sha1 of `blob <len>\0`+content = `ee85acc60e4a6fe2ffcc424eae4642de42f5a955`) against a spec whose header reads "revision 9", so the pinned constant is already correct and the executable check at :103-105 passes — ten sites assert otherwise, but the plan states the truth at :44's and :5461's first clauses, so the repair is in scope and no bytes move. Nothing in the set yields a spec-rev-9 non-conformant artifact or a new false green.

## Conditions

### C1. [MAJOR] Both sanctioned MCP-golden re-freeze scripts die before writing a byte — reproduced live

**Target:** Task 9 Step 4 (plan :3710-3723) and Task 20 Step 3 (plan :5545)

I ran the pinned script's helper import and call under `uv run python` outside pytest and got `AssertionError: {'jsonrpc': '2.0', 'id': 1, 'error': {'code': -32600, 'message': 'server not initialized'}}`. `_live_output_schemas()` (tests/conformance/test_mcp_output_schema_golden.py:95-101) asserts `"error" not in resp`; the handshake opt-out lives ONLY in that module's autouse pytest fixture `_handshake_preopened` (:48-61), which monkeypatches `JsonRpcServer.__init__` to `require_handshake=False`. `WardlineMCPServer` constructs `JsonRpcServer(...)` with no such argument (mcp/server.py:4730) and `protocol.py:37,103-104` default it True and reject every non-`initialize` method. Task 20 Step 3 says verbatim "Re-freeze ... with the SAME scratch script as Task 9 Step 4", so BOTH of the plan's exactly-two sanctioned re-freezes are unexecutable and there is no third attempt budget. FIX: prepend to the Task 9 Step 4 script, before importing the test module, a monkeypatch mirroring the autouse fixture — bind `_ORIG = JsonRpcServer.__init__` and replace it with a lambda forcing `require_handshake=False` — and add the sentence "Do not hand-edit the golden if the script fails — regenerate it from the live surface, per that module's RE-FREEZE PROCEDURE", because hand-editing is the nearest repair and that module's header forbids it by name. Then either repeat the patched script at :5545 or have :5545 point at the patched Task 9 block by name.

### C2. [MAJOR] S0 close condition (iv) gates the close on a measurement defined nowhere in the repo

**Target:** Final verification plan :6020 and close condition (iv) at plan :6033; originates at spec :190

`grep -rIn --exclude-dir=.git "frozen-oracle|blinded negative|negative control|over-approximation probe"` returns only spec :190, the plan's :6020/:6033, the originating condition C21 in docs/superpowers/plans/2026-08-10-s0-rev6-go-no-go.md:231-235 (which mandates the re-run in the same undefined terms and defines nothing), and two unrelated hits. The four suites, the probe's "exactly two symbols" and the blinded control are referenced as known prior artifacts and exist as none. WHICH four is genuinely underdetermined: Global Constraints :35's no-regeneration list names seven artefacts and its value-census paragraph reduces them to a five-path marker-bearing subset. Condition (iv) is one of five conjuncts gating the close of `wardline-5a795253f1`, so this is an executable close gate with no executable content, and the likely outcome is the no-op receipt :6020's own second sentence warns against. FIX (spec and plan together): in spec :190 and plan :6020, enumerate the four suites by path or by exact `uv run pytest` invocation; state the probe's two patched symbols; define the control as the perturbed symbol plus the delta that must appear; state the pass condition as "the four suites show zero delta AND the control shows a non-zero delta in the same session"; re-cut :6033(iv) to point at that enumeration. Calibration for the owner: the substantive property is already protected — :35 forbids regeneration of all seven artefacts by path and requires a green full suite after every wardline task — so the realistic damage is a close receipt that overstates what it measured, not an undetected golden move.

### C3. [MAJOR] The spec-blob pin is labelled revision 8's, with a re-pin "owed", at ten live sites — including the Custody verdict and the execution preflight

**Target:** plan :11, :19, :21, :23, :27, :44 (later clause), :100, :111, :5461 ("after it has been re-pinned" clause), :6041; the constant itself at :104 is CORRECT

Measured, not inferred, and without invoking git: the working-tree spec is 159,200 bytes and `sha1(b'blob %d\0' % len + data)` = `ee85acc60e4a6fe2ffcc424eae4642de42f5a955`, exactly the constant at :104, and that file's header (spec :4) reads "**Status:** DESIGN, revision 9". The pinned blob IS revision 9's and no re-pin is owed. The executable check at :103-105 therefore PASSES. Ten sites say otherwise. Two are load-bearing: :27 is the **Custody verdict** and makes execution conditional on a re-pin "which the preflight constant does not yet carry" — a precondition already satisfied that cannot be re-performed; and :100 is the paragraph immediately above the constant and primes the reader to read a PASSING check as the drift case, inviting the STOP-and-re-review it mandates. :21 is self-contradictory inside one sentence (the blob "**is** revision 8" AND "revision 9 was committed at `c08834ed`"; "the honest residue moves one revision along rather than closing" AND "that residue is now **discharged**"), and :44's "all eight pin sites were re-cut" is a false self-audit. FIX: state at every site that blob `ee85acc60e4a6fe2ffcc424eae4642de42f5a955` at commit `c08834ed` IS revision 9's — the governing revision — and that no re-pin is owed; delete every "re-pin from revision 9's commit" clause; delete :100's prediction that the check fails on a rev-9 tree, keeping only the STOP-and-re-review-on-drift discipline and the standing prohibition on re-deriving from a working-tree hash; strike the phantom precondition from :27, leaving "the spec blob check must pass at the moment of execution"; at :19 and :6041 mark the correction as a rev-3.7-and-later amendment so what each revision believed survives as record. Do NOT adopt any prior candidate's site count — one candidate's list says "ten" and enumerates eleven. Replace :44's count with the enumeration above, and verify by grepping the digest, the revision word AND the premise ("re-pin", "awaiting", "still names", "the moment that commit exists") — which is the lesson :6041 already records from the rev-3.4 pass and which this residue defeated again.

### C4. [MAJOR] Spec rev 9's explicitly withdrawn "no waiver or baseline row can even be written" over-claim survives at five live sites, one of which ships in CHANGELOG.md and one of which is posted to Filigree

**Target:** plan :3473 (Task 8 Step 4 preamble), :3499-3502 (the guard test's NAME and comment), :3579 (the `wardline-c32e5d1420` add-comment text), :4890 (Task 18 Step 1's CHANGELOG body), :6021 (Final verification)

False in source: `add_waiver` (core/waivers.py:157-187) takes fingerprint/reason/expires and never receives a `Finding`; `_build_baseline` (core/baseline.py:268-289) builds `Baseline(frozenset[str])` from a hand-authored `entries:` list with no kind check. So a waiver or hand-authored baseline row naming this FACT's fingerprint IS accepted and is simply inert at `apply_suppressions`' `Kind.DEFECT` short-circuit (core/suppression.py:99-102). Spec rev 9 :167 withdraws the wider claim in terms — "An earlier drafting said none could be written at all; that was an over-claim against the shipped machinery and is withdrawn" — and names deletion of the guard on that premise as the hazard. Two of the five sites are worse than prose: :4890 is a paste step that ships the false claim in `CHANGELOG.md` under `## [Unreleased]` as a user-facing statement about a security tool's suppression surface, and :3579 commits it into Filigree as a permanent scope note. The test NAME at :3499 (`test_residual_fact_cannot_be_written_into_a_baseline_document`) is the site a future maintainer reads first, and it sits adjacent to the sibling guard that deliberately constructs BOTH `Baseline(frozenset({_FP_A}))` and a `Waiver` on that fingerprint — exactly what rev 9 mandates. FIX all five in one pass; a fixer who does four prose edits and leaves the name has not closed it. Correct claim everywhere: no baseline row is ever GENERATED for it (because `build_baseline_document` filters through `_is_baselineable_finding`); a waiver or hand-authored row MAY name its fingerprint and is INERT. Rename the test to `test_residual_fact_is_never_generated_into_a_baseline_document` and leave both test bodies unchanged.

### C5. [MAJOR] The shape-gate short-circuit is still justified on P13's repository-scoped waiver ceiling at three sites — including a docstring committed into shipped source

**Target:** plan :710 (the verbatim `call_shape_offences` docstring typed into src/wardline/scanner/marker_reader.py), :1973 (Task 5 Step 3 implementation directive), :3150 (Task 8's canonical ruled-ordering paragraph)

Spec rev 9 :171 replaces this justification in terms: "**What makes that acceptable is the shipped secure default, not a ceiling** ... §12's P13 waiver ceiling is a secondary and weaker point ... a **repository-scoped** bound ... which says nothing about whether this particular one was reviewed, and it must not be asked to carry this concession alone." The pass is half-applied: :23 and :4309 carry the corrected rev-9 wording, these three do not, and plan :21 claims the move was made. :710 commits the withdrawn claim into source, where it becomes the authority the next refactorer reads. :3150 is the worst: it self-describes as canonical ("stated once here so no step has to infer it ... **Write it that way**") and names P13 explicitly ("an explicit reviewed act against P13's fixed ceiling"), so a fixer who patches :710 and :1973 leaves the authority standing. FIX: adopt :4309's wording wholesale at all three — "...justified on noise avoidance plus the shipped **secure default**: under `trust_suppressions=False`, `run_scan` rebuilds `gate_population_findings` with an empty `Baseline`, an empty `WaiverSet` and `judged=None`, so a waived or baselined `PY-WL-130` still trips `--fail-on`, and the site loses its signal only under an explicit `--trust-suppressions` operator decision (spec rev 9 §4.2.1; P13 is a repository-scoped ceiling and never bore this weight) — not dominance." At :3150 additionally strike "against P13's fixed ceiling" and add the residual-risk sentence spec :171 mandates be "stated rather than argued away": under `--trust-suppressions` with `PY-WL-130` waived the site retains only a waived, non-gating DEFECT and no unsuppressible signal. Verified in source: run.py's `GateSuppressionPosture.UNSUPPRESSED` arm does rebuild the gate population exactly as described, so the replacement justification is sound.

### C6. [MINOR] Task 5 Step 3's pinned `_match` body is a mypy-strict `[assignment]` error in src/, red at Final verification six tasks later

**Target:** plan :2061 (`levels[la.arg_name] = read.level`), against `levels: dict[str, TaintState] = {}` at :2023

Task 2 types `LevelRead.level` as `TaintState | None` (plan :403, :580-582). The guard above the assignment narrows `read.verdict`, and mypy performs no correlated narrowing onto the `level` field, so the assignment is `TaintState | None` into `dict[str, TaintState]`. Reproduced with `uv run mypy --strict` on the exact shapes: `error: Incompatible types in assignment (expression has type "TaintState | None", target has type "TaintState")  [assignment]`. pyproject.toml:115-118 sets `strict = true` over `src/wardline` and `tests`, and the overrides block disables `assignment` for `tests`/`tests.*` ONLY — never for src/. The shipped `_match` avoids this by narrowing the value itself (decorator_provider.py:413-416, `if lvl is None: break`). Task 5's own Step 4 runs pytest only, so the error first surfaces at Final verification's `uv run mypy` (plan :6019). FIX: one line in the same function — insert `assert read.level is not None` before the assignment, or bind and test `level = read.level` — keeping `LevelVerdict.RESOLVED` as the semantic gate.

### C7. [MINOR] Task 2 Step 5's pinned test import block is un-sorted — ruff I001, red at Final verification

**Target:** plan :890-892

Reproduced against the repo's own ruff config: the pinned header places `from wardline.core.run import run_scan` before `from wardline.core.registry import MarkerCallForm`, and `uv run ruff check --select I` returns `I001 [*] Import block is un-sorted or un-formatted`. pyproject.toml:112-113 selects `["E","F","I","UP","B","SIM"]`, there is no `[tool.ruff.lint.isort]` override, and `tests/` is not in `extend-exclude`. Final verification (:6019) runs `uv run ruff check src tests` as a green gate. FIX: swap the two lines so `registry` precedes `run`. Note this is a lapse in one block, not a document-wide blind spot — Task 2 Step 2's own import instruction at :822 is explicitly isort-aware. Scope caveat: this is the only pinned block I ran through ruff.

### C8. [MINOR] No step closes `wardline-2b2a6cddfa`, though close condition (ii) requires it CLOSED — and Final verification asserts a status the tracker will contradict

**Target:** plan :3576 (Task 8 Step 10) and :6032; close condition (ii) at :6033; Filigree discipline at :115-119

Every occurrence of `wardline-2b2a6cddfa` in the plan was enumerated: the only Filigree command touching it is `filigree --actor codex add-comment wardline-2b2a6cddfa` at :3576 — a comment, not a close. The Filigree discipline list names an owning action for `wardline-4928b75782` and `wardline-5a795253f1` and says only that this sibling "closes inside S0", naming no step. Meanwhile :6033(ii) requires it CLOSED as a precondition of the S0 close, so the close gate names a state no step produces — in a plan whose entire rev-3.6 defect class was unowned hand-offs. Separately, :6032's "both are genuinely still open **at that moment**" is false in the branch its own "if not already closed" clause admits: Final verification runs after Task 8, which discharges the sibling — violating the plan's own rule at :116, "do not assert a status the tracker can contradict." FIX: rename Task 8 Step 10 to own the close, add the close command alongside the existing add-comment gated on Step 9's committed exit-code repro and Step 12's green, add the matching bullet to the Filigree discipline list, and re-cut :6032 to the by-owner conditional form :116 already mandates. Bounded severity: the orchestrator reads condition (ii) at the close gate and would catch the open ticket there.

### C9. [MINOR] Task 2 Step 3's `examples_violation` code block has no lead-in naming its destination, leaving a dangling "and `METADATA.examples_clean`" clause

**Target:** plan :851-855 (the bare fence) and :857

Line :849 ends with the three behaviour deltas; :851-855 is a bare fenced Python block with no preceding instruction; :857 opens "and `METADATA.examples_clean` — **two edits, both in this step**". A whole-plan grep for `examples_violation` returns only :2530, :2693, :3827 and :3841, all in other tasks, so the destination is stated nowhere in this step and the "and" has no antecedent. The inferred target is confirmed in source: src/wardline/scanner/rules/invalid_decorator_level.py:44-51 holds `examples_violation`, whose third entry is the structurally identical aliased-decorator case. Sub-items (a) and (b) at :859-861 are themselves labelled as the two `examples_clean` actions, so the "two edits" count only parses if the unattached block is the `examples_violation` append — further evidence the lead-in was dropped rather than omitted. FIX: insert the missing lead-in before the fence naming `METADATA.examples_violation` and the APPEND action explicitly. This is a step whose whole emphasis is that every edit name its target file and attribute.

### C10. [MINOR] One dead plan-internal line coordinate survives rev 3.7's named-anchor re-cut and resolves to unrelated text

**Target:** plan :1358 ("`FORM5_CASES` above already carries ten and :960 declares the list open-ended")

Plan line 960 is `#  spec :83's "so no second frozen marker is left behind".` — a comment inside `_level_value`'s body about `to_level=` being argument-name-agnostic. The text that actually declares the list open-ended ("The eight are a spec MINIMUM, never a maximum: append any further row BELOW the added block") sits at :996-997, immediately above `FORM5_CASES = [` at :998. It is plan-internal, not a spec coordinate (the spec is 551 lines) and not a source coordinate (every other `:NNN` in the plan carries a filename); a filename-agnostic coordinate grep confirms it is the only survivor of the nine self-pins rev 3.7 claims at :21 to have re-cut to named anchors. FIX: replace with a named anchor — "...already carries ten, and the `FORM5_CASES` preamble comment declaring the eight 'a spec MINIMUM, never a maximum' keeps the list open-ended...". Cosmetic: the sentence's directive ("Name it by function, never by ordinal") stands without the pointer and no build, test or artifact turns on it.

### C11. [MINOR] Task 5 Step 1 calls decorator_provider.py:196 "the provider's only `except ValueError`"; there are two

**Target:** plan :1967

`grep -n "except ValueError" src/wardline/scanner/taint/decorator_provider.py` returns :196 (inside `_read_level`, wrapping `TaintState(token)`) and :234 (inside `_closure_identity`, wrapping `items.append(_seed_value_identity(...))` with an `"<empty-cell>"` fallback). The material conclusion is unaffected and correct — :234 is on the provider-fingerprint path, nowhere near `taint_for` — so the Step 1 amendment stands unchanged. FIX: narrow the claim to "no `except ValueError` wraps the reader call on `taint_for`'s path — the only one in that path is :196, inside `_read_level`; the file's other, :234 in `_closure_identity`, is on the fingerprint path". Cost of leaving it: an implementer who verifies the plan's "verified in source" claim re-derives the whole disposition.

### C12. [MINOR] Task 9 Step 3.3 justifies adding the new summary keys to `required` with a false statement of JSON Schema semantics

**Target:** plan :3705

The parenthetical claims "a key present in `properties` alone is still rejected" under `additionalProperties: False`. That is wrong: `additionalProperties` constrains only instance properties NOT matched by `properties`/`patternProperties`, so a key in `properties` but absent from `required` is optional and validates fine when present. The second half (a key absent from `properties` fails outright) is correct. No wrong artifact this pass — the instruction produces the right schema — but a later editor trimming the schema on this rationale drops the two keys from `required`, and the conformance oracle then stays green over a payload that has silently stopped emitting `unreadable_marker_values`, losing spec §4.2.1 condition 5's only surface pin. FIX: replace with "(the block is `additionalProperties: False`, so a key absent from `properties` fails validation outright; `required` is separate and load-bearing — tests/conformance/test_mcp_structured_output.py validates the live `structuredContent` against this schema via `jsonschema.validate`, exercising `decorator_coverage` directly, so only `required` pins that every renderer actually emits both counts)". Verified: that test imports jsonschema at :25 and `_validated` runs the validation at :131-139.

### C13. [MINOR] Task 2 Step 3 asserts PY-WL-130 owns malformed markers four tasks before PY-WL-130 exists, and the discriminating hand-off case is pinned by no test

**Target:** plan :849 ("if a marker is malformed, PY-WL-130 owns it and PY-WL-114 is silent"); the missing regression belongs at Task 6

Two durable items. (1) :849 is false where it is written: the plan itself says at :771 that PY-WL-130 does not exist until Task 6, which is the rule's owning task (:2099). Shipped PY-WL-114 (rules/invalid_decorator_level.py:145-212) has no shape gate today — it fires on a readable-but-invalid token regardless of any sibling keyword or positional argument. (2) The PY-WL-114 → PY-WL-130 hand-off for the DISCRIMINATING case — a builtin marker that is BOTH shape-malformed AND carries a readable-but-invalid token, e.g. `@trusted(level='ASURED', audit=True)` — is pinned by no test anywhere: Task 6's `test_literal_non_string_splat_key_is_shape_defect_only` uses `**{1: 'ASSURED'}`, where shipped PY-WL-114 is already silent, and Task 14's `malformed_shape_and_unreadable_value` row uses an unreadable value rather than an invalid token. That gap ships. The Tasks 2-5 window in which such a site is silent on every channel is real (the released reader already drops the seed at decorator_provider.py:165-186) but is intra-plan, pre-release and closes at Task 6. FIX: re-cut :849 so it does not assert a channel that does not exist at that commit; announce the window the way the two sanctioned carried reds are announced; and add a Task 6 regression asserting PY-WL-130 fires and PY-WL-114 does not on `@trusted(level='ASURED', audit=True)`.

### C14. [MAJOR] SPEC: §4.2.1's star-import poison is scoped "at top level", so a nested star import from a project module escapes every channel and form 5 still mints a seed

**Target:** spec :145 (the star-import census row); plan Task 3 Step 2's poison predicate at :1495-1503 faithfully implements it and is NOT the defect

Verified end to end in source. A module-level-but-nested star import (inside `try:` or `if`) is legal and compiles, but is not a direct element of `Module.body`, so the census poison predicate — which iterates `for stmt in tree.body` exactly as the spec's "at top level" scope directs — does not fire. It escapes the census occurrence walk too (`if alias.name == "*": continue` in the Import branch); it escapes `build_import_alias_map`, which iterates `ast.iter_child_nodes(tree)` (scanner/ast_primitives.py:49) — which is also why the plan's `test_census_poison_agrees_with_build_import_alias_map` goes green over it; and when the star's target is a PROJECT module it escapes `diagnose_unknown_imports`, which short-circuits at `if mod in project_modules: continue` (scanner/diagnostics.py:334, :369). Net: no poison, no occurrence, no diagnostic on any channel, and form 5 resolves a constant the star may have rebound. Narrowing: the rebinding hazard requires a NON-underscore constant (or an `__all__` naming the underscore one), since `import *` does not rebind underscore-prefixed names. FIX: change spec :145 from "at top level" to "anywhere in the module", and change Task 3 Step 2's predicate to `ast.walk(tree)` — matching the deliberate asymmetry the `global` walk already has — then amend the agreement test to pin agreement over top-level stars only, plus a row asserting the census is deliberately stricter. Recording it instead as a spec §14 rejected alternative with its reopening precondition is acceptable, provided the project-module nested-star shape is named as the one with no signal on any channel.

### C15. [MAJOR] SPEC: condition 4's collision concession is licensed by an argument that does not reach it — a fingerprint collision is a gate-eligible ERROR regardless of the colliding channel's severity

**Target:** spec :168 (§4.2.1 condition 4, "acceptable here because the channel is `Severity.NONE` and never gates")

Verified in source and the plan-side linkage traced. `build_collision_findings` (scanner/diagnostics.py:106-193) groups the full emitted set by `Finding.fingerprint`; any group whose members differ under `to_jsonl()` emits one `WLN-ENGINE-FINGERPRINT-COLLISION` at `Severity.ERROR`, `Kind.DEFECT`, `location=ENGINE_PATH`, and core/suppression.py:102 downgrades a lineless DEFECT only when `path != ENGINE_PATH` — so it trips `--fail-on ERROR`. The residual FACT does reach it: Task 8 appends to pipeline.py's `parse_findings` → `ParseProjectOutput.parse_findings` (:285) → analyzer.py:314 → :1165 → `build_collision_findings(findings)` at :1274. So "never gates" is irrelevant to "acceptable", and "a collision merges two adjacent diagnostics" holds only when the colliding findings are byte-identical under `to_jsonl()` — a property condition 4 never establishes, since it fixes the fingerprint preimage exhaustively but says nothing about the FACT's location or message. An implementer working from the governing text and mirroring PY-WL-114 (which the same paragraph cites, and whose granularity is per-decorator) would anchor at the decorator's line, and two stacked `@trusted(level=DYN)` decorators would then turn a green scan red. Plan Task 8 is SAFE and supplies the missing constraint — `location=ent.location` pinned, and `value_key = unicodedata.normalize("NFC", value_text)[:200]` used as the message, the `value` property AND the fingerprint's fourth part — so both admitted collisions are byte-identical and benign. The defect is in the governing text, which spec :8 says governs in conflict. FIX (cheaper option): make the entity-scoped location and the preimage-derived message NORMATIVE in condition 4 and name `scanner/diagnostics.build_collision_findings` explicitly, so the next reader cannot re-derive "a collision is harmless here" from the channel's severity. The alternative — a within-def ordinal as a fifth preimage part — contradicts condition 4's "there is no fifth" and would move the plan's pinned fingerprint.

## Ready to execute

Two separate answers; do not read either as the other.

CAN AN ENGINEER START TASK 1 TODAY? Yes. Unhedged. Task 1 (registry grammar — `ArgKind`, call form, immutable kwargs) is touched by nothing in the condition list. The spec-blob pin gates Tasks 19, 20 and 22 only and the plan says so in three places (:44, :100, :111) — and I measured the constant myself: it is already correct and the check at :103-105 passes. The Custody verdict at :27 attaches two live preconditions to execution as a whole; both are satisfiable today (the Legis clean-target blocker was resolved at `a117a21`, the blob check passes), and :27's remaining gate — "Rev 3.5 is not cleared for execution until it is re-reviewed" — is discharged by this verdict. Seven independent derivations walked Tasks 1 and 2 statement by statement and found no defect in Task 1.

IS THE PLAN SHIPPABLE AS A WHOLE? No, not yet — 15 conditions, all enumerated above, none blocking. Shortest path: close conditions 1-5 (the five MAJORs) before an engineer reaches the tasks they touch, and the rest in the same editing pass. There is real slack in the ordering, and it is worth using: condition 1 (the re-freeze script) is not needed until Task 9; conditions 3-5 are prose and pins; condition 2 is not needed until the close. Only conditions 6 and 7 (mypy and ruff, both reproduced) sit inside Tasks 2 and 5 and would surface as reds at Final verification if left — both are one-line fixes.

CAVEAT ON STARTING BEFORE FIXING: if execution begins now, condition 5 (:710) is the one to close first, because its text is committed into `src/wardline/scanner/marker_reader.py` at Task 2 and becomes shipped source; and condition 4's :4890 must be closed before Task 18, because that step pastes a false claim about a security tool's suppression surface into `CHANGELOG.md`.

## Audit caveats

SELF-REPORTED COVERAGE GAPS ACROSS THIS CYCLE'S DERIVATIONS:

1. Lint and type coverage of pinned code is near-zero. Exactly one pinned block was run through ruff and one through mypy — both produced findings (conditions 6, 7). The remaining ~40 pinned snippets were READ, not linted or type-checked. There is no basis for asserting they are clean, and the two that were checked both failed, which is a poor base rate.

2. Tasks 10-13 and 15-18 were SAMPLED, not derived statement by statement: Files lists, line anchors, API existence and spot-simulation only. Two self-verifying steps were never re-derived — Task 10 Step 4's corpus-manifest reconciliation and Task 13 Step 1's fingerprint-digest rewrite.

3. Cross-repo Tasks 19-23 were verified at the API-and-anchor level (loomweave `wardline_descriptor.py`, warpline `_attest.py`, legis `ingest.py`, the seam-registry non-touch pin, the Rollout Fence probes). No cross-repo step was executed.

4. NEVER CHECKED BY ANY PASS THIS CYCLE, and unfixable by review: the four frozen-oracle suites, the two-symbol over-approximation probe and the blinded negative control in close condition (iv). This is not an omission of effort — a whole-repo grep shows they are defined nowhere, so the gate cannot be checked until condition 2 defines it. It is the only close-gate conjunct with no executable content.

5. Both sanctioned MCP-golden re-freezes were reproduced as FAILING. Nothing downstream of them (the re-frozen golden's bytes, the new `VENDORED_BLOB_SHA`) has been or can be verified until condition 1 is applied.

6. No pass executed the plan. Every claim about what a step produces is a derivation from the pinned text plus the current source tree, not a build. The exceptions, run read-only in scratch directories: the hole-3 exit-code repro specimen (confirmed — literal-level form exits 1, `_SVC_LEVEL` form exits 0 today), Task 5's malformed-sibling prediction, the census/star-import chain, the CPython lineno claims at 3.13.1, the NFC and `ast.unparse` behaviours, and the re-freeze script failure.

7. Line-number self-pins were spot-checked, not exhaustively resolved. At least three are off by one or two and resolve to the named entity anyway (`context.py:176` should be `:177`; `test_rule_examples_meta.py:64` vs the filter at `:66`; `registry.py:22` vs `:21`). Condition 10 is the one dead pointer found; a full pin sweep was not run.

8. One item was observed and deliberately not filed as a defect: plan :19 and :1383 cite the per-task path gate as "Global Constraints :5", but the quoted sentence lives in the Git-discipline blockquote at line 5, not in the Global Constraints section at :33-50. The coordinate resolves; only the section label is wrong.

9. Per the brief, the following were NOT re-examined and are excluded from this verdict: the scope split and PRD-0003 criterion 6's ownership, the shape gate's short-circuit as a design ruling, the seventh `decorator_coverage` key inside the sanctioned re-freeze, the deferred G2 narrowing, the absent-census raise's builtin-only scoping and `WLN-ENGINE-FILE-FAILED` disposition, and the builtin-only status of `call_shape_offences` and PY-WL-130.

10. Five candidate findings were REFUTED on the file this round and should not be re-filed: §4.4's family-membership predicates (the accept predicate IS stated positively, with a total `unparseable_args` catch-all); the "zero bare-`Name` level values" census (the three cited counterexamples are Python inside string literals, not marker call sites); Task 3 Step 5's multi-target-assignment assertion (it executes and discriminates); Task 2 Step 2's `ModuleCensus` import (instructed in prose at :828, same step); and the `--new-since` residual-risk omission (the FACT is `Severity.NONE` and was never a gate signal in any mode).