# S0 go/no-go — plan rev 3.4 and declaration-surface spec rev 6

`Date: 2026-08-10` · Audit: 13 agents (6 plan-slice census, 1 frozen-interface probe, 1 bet-scope, 4 refutation, 1 synthesis)
· Candidates: 84 · Survived refutation: 76 · Killed: 6

## Verdicts

- **Spec (rev 6, governing design):** GO-WITH-CONDITIONS
- **Plan (rev 3.4, executable):** NO-GO

### Headline

Spec rev 6 is sound and implementable — GO with six conditions, four of them owner calls or small under-specifications, none of them design defects. Plan rev 3.4 is NO-GO, and it is overdetermined: a whole-plan grep returns ZERO occurrences of "form 5", "reference site", "UNREADABLE-MARKER-VALUE", "SeedContext", or any Rust frontend file, so rev 6's engineering is not stale in the plan — it is absent. Three independent NO-GO triggers each suffice on the pre-committed criteria: Task 2's frozen `level_token`/`read_level` signatures make P9 — Task 2's own named QE deliverable — unprovable (spec :160: "the interface as frozen cannot pin P9 as written"), so an implementer ships a green agreement receipt over a reader that does not implement the grammar; Task 12's `Kind.DEFECT` filter at :2674 plus the pure-absence branch at :2679 build a false green into the guard whose entire purpose is proving there are none; and Task 6's three-arm `taint_for` branch plus "Nothing else in `taint_for` changes" makes the residual population structurally unreachable. The condition list also cannot be closed as required for GO-WITH-CONDITIONS — three items (the per-module census type/builder/carriers, the residual FACT carrier/emission/five soundness conditions, and the §4.4 Rust split) are new engineering with no owner, which is not "textual, pin-level, or sequencing", and spec :204 explicitly withdraws its own exhaustiveness claim, so no list built against it can be certified complete.

### Bet position (PRD-0003)

A GO on the plan would not be a GO on PRD-0003, and there is no GO on the plan. Executing rev 3.4 verbatim closes exactly one of the bet's three holes — `wardline-4928b75782`, and only its call-shape half per the rev-3.3 re-scope — while the plan *instructs* the implementer at :106, :107, :4092 and :4098 to leave `wardline-2b2a6cddfa` open, and touches no Rust file anywhere in 4099 lines, leaving `wardline-b857b50b54` unowned. G2 therefore reads at least 2 at the close receipt against a TARGET of 0, and PRD-0003's own falsification condition fires. Two of the three holes remain unowned by any plan: hole 2 (Rust marker shape) and hole 3 (unreadable level value). The bet is AT-RISK, not CANNOT-REACH — spec §4.2.1 and §4.4 now specify both fixes completely, so no design is owed; what is missing is plan steps. The precise novelty is narrower than "rev 6 falsified the product records", and should be stated that way: PRD-0003 :140-144 already said `wardline-2b2a6cddfa` is "the item that genuinely needs a plan" and that only `wardline-4928b75782` was covered, and PDR-0017's Decision is scoped verbatim to `wardline-4928b75782` ("S0 executes as-is against plan rev 3.4 **for `wardline-4928b75782`**"). What neither anticipated is that rev 6 places form 5 inside Task 2's **engine-core produced interface** rather than beside S0 — so the hole-3 fix is not additive work alongside a green S0, it re-cuts S0's own frozen reader and its P9 receipt. Three things stand between here and a readable metric and none are engineering: the owner must rule where the two siblings land (spec :470 puts both in S0; PDR-0017 explicitly rejected holding S0 for rev 6), must rule or explicitly defer the G2 narrowing (spec :174-179, unratified, and it changes only criterion 2's verdict), and must record a dated metrics.md reading — the series still reads G2 at **1** (2026-08-09, PDR-0012) while PRD :30 baselines at **3**, so criterion 2 would today be read against a value that was never written down.

## Blocking findings (11)

### B1. S0 scope excludes two of the bet's three holes; the governing spec places both inside S0

**Plan:** :106, :4092, :4098 (and :107 tail)

**Why blocking:** Plan :106 says the two siblings "are out of S0 scope and must remain open at close"; :4098 lists them as "Deliberately NOT in S0 ... both live after S0"; :4092 closes S0's ticket `wardline-5a795253f1` on Task 21 alone. Spec :470's S0 row places `wardline-2b2a6cddfa`, `wardline-b857b50b54` and form 5 with its census inside S0 under that same ticket, and spec :8 rules that where the two conflict the spec governs. Executed verbatim, the tracker banks a green S0 stage over two live fail-open holes — the tracker itself becomes the false green. Four sites, not one: plan :4099 records that rev 3.4's own inventory built by grepping a single token missed two of five sites.

**Fix:** Re-cut :106, :4092 and :4098 so `wardline-4928b75782` still closes after T6 as the call-shape half only (that honesty discipline is correct and must be preserved), while `wardline-5a795253f1` closes only when all three holes close, each with its committed exit-code regression test (spec :188), the residual FACT's five soundness conditions each pinned by a named test (spec :164-168), and the zero-delta blast-radius re-run recorded (spec :187). Adding the siblings to the close criteria without adding the tasks only relocates the contradiction — see the census, carrier and Rust conditions.

### B2. Plan line 107 is a standing prohibition on exactly the change revision 6 mandates

**Plan:** 107

**Why blocking:** Under the plan's Filigree discipline heading, bolded "Settled in rev 3.4 — do not re-open", the bullet closes "`wardline-2b2a6cddfa` stays open for both." Spec :194 names this line first in its supersession list — "forbids exactly this change" — and PDR-0018 supplies the owner authority reversing it. This is the one site phrased as a prohibition rather than a scope statement, so a conscientious implementer who noticed §4.2.1 would classify form-5 work as scope creep and STOP under :42's no-scope-broadening discipline. Left standing it also reverses the T12 docstring and matrix fixes other conditions require.

**Fix:** Delete the "Settled in rev 3.4 — do not re-open" framing, the two-gap T12 matrix scoping, and "`wardline-2b2a6cddfa` stays open for both". Replace with a "Re-opened by PDR-0018 / spec rev 6" bullet recording that the ticket is re-opened by owner decision and closed in S0 by form 5 plus the residual FACT. PRESERVE verbatim the one clause spec :199 calls "unchanged and reinforced": an unreadable level value must never appear in any rule's `examples_clean`.

### B3. Task 2's frozen shared-reader interface cannot express form 5, so P9 — Task 2's own QE deliverable — is unprovable

**Plan:** 385, 388

**Why blocking:** Under "Produces (all public, exact signatures — Tasks 4–6 import these)", `level_token(value, alias_map)` and `read_level(deco, arg, *, declared, allowed, default, alias_map) -> TaintState | None` carry no census, no reference site, no `shadowed_roots`, no builtin/custom discriminator, and a return type that collapses spec :121's UNREADABLE+FACT and spec :144's READS-then-rejects into one bare `None`. Spec :200 names both lines; spec :160 states flatly "the interface as frozen cannot pin P9 as written." Plan :367 titles the task "(P9)" and :4096 maps "P9 → Task 2". An implementer builds to these signatures, writes a green agreement test, and records a QE receipt asserting two readers agree on a grammar neither implements — a false receipt on the exact property P9 exists to prove. The only repair that keeps the signatures compiling is a defaulted-empty census, which spec :155 forbids by name because it ships the one-sided false green spec :153 describes.

**Fix:** Re-cut both entry points to take the per-module census and the reference site (the decorated `def`/`async def` statement) as required keyword parameters with NO default (spec :155), plus `shadowed_roots` (spec :159). Retain line 385's "STRICT: alias-resolved `wardline.core.taints.TaintState` receiver or str literal", which spec :154/:200 explicitly do not supersede. State a mechanism keeping form 5 builtin-only (spec :105/:119) — either a required non-defaulted discriminator on both entry points or an explicit caller-side gate in Task 4; the plan must name one. State how the caller distinguishes UNREADABLE from READS-then-rejects and how the `(argument name, ast.unparse(value node))` pair reaches the emission loop (spec :167, :170) — a discriminated return type is one solution, composing the already-produced `extract_keywords` with `level_token` is another; pick one. Pin spec :156's absent-vs-empty split: a census present but holding no qualifying entry, or not holding the reference site among its form-5-eligible ones, returns `None`; a bare `Name` in a LEVEL slot with no census at all is a plumbing defect and the reader RAISES (rule side: `WLN-ENGINE-RULE-FAILED` ERROR DEFECT; provider side: propagation out of the parse pass).

### B4. P9's CASES table pins the superseded bare-Name verdict and cannot express six of revision 6's eight mandated cases

**Plan:** 697-715 (row at 705)

**Why blocking:** Row `("LEVEL", {}, None)` at :705 is the frozen behavioural pin for the whole bare-`Name` case and encodes the pre-rev-6 answer. Under spec :156 that single row splits three ways — RESOLVES, `None`, and raise — which a 3-tuple cannot represent. The harness at :712-715 does `ast.parse(expr, mode="eval").body`, a naked expression with no module, no bindings and no decorated statement, so it cannot exercise a census or a reference site at all; the two `run_scan` agreement tests at :766-801 cover only re-exported and aliased `TaintState`. Spec :160 mandates eight cases and singles out star-import poison and custom `BoundaryType` — neither has a row — as "the two cases whose failure mode is silent cross-reader disagreement". Spec :444 makes the interface amendment and the table widening one change, not two.

**Fix:** Widen the table in the same pass as the :385/:388 re-cut to hold at minimum spec :160's eight cases: bound; unbound; method-or-nested-`def` reference site; conditionally-defined module-level `def` reference site; two-occurrence census; `global`-declared name; star-import-poisoned module; custom `BoundaryType` LEVEL argument (asserting `WLN-ENGINE-UNPROVABLE-BOUNDARY` + `UNKNOWN_RAW` and never the residual FACT). Drive the six census/reference-site cases through `run_scan` on the analyser's real construction path, asserting on both the rule-side finding set and `result.context.declared_qualnames`; spec :156 forbids a hand-built context, so state explicitly that a census literal constructed inside a parametrised test does not satisfy P9. Add a `pytest.raises` row for the no-census plumbing defect (plan :503-504's existing `raise ValueError` for an undeclared arg is the in-module precedent). Retain the expression-level table only as a supplementary unit check taking an explicit census and reference site.

### B5. No task creates the per-module census type, its parse-loop builder, or the SeedContext/AnalysisContext carriers

**Plan:** new task after Task 2; Files lists at 369-376 and 880-886

**Why blocking:** A whole-plan grep returns zero occurrences of `SeedContext`, and the four `census` hits are the unrelated 2026-08-09 `to_level` fixture census. Verified in source: `SeedContext` (`src/wardline/scanner/taint/provider.py:28`) has `module`/`alias_map`/`project_modules` and no census; `AnalysisContext` (`src/wardline/scanner/context.py:43`) has no census field though its `module_bindings: Mapping[str, SinkBindings] = field(default_factory=dict)` at :122 is the precedent spec :158 names; `pipeline.py:166` is the sole `SeedContext(...)` construction, inside the one-per-module parse loop spec :157 designates as the build site. The shared reader's required census parameter therefore has no type, no producer and no transport, and Tasks 4-6 import an unbuildable signature. The two dispositions available to an implementer are both forbidden: a defaulted-empty census (spec :155/:156) or the reader's raise, which propagates out of a parse loop whose `except` catches only `SyntaxError`, `UnicodeDecodeError`, `OSError` and aborts the scan.

**Fix:** Add a task owning: (a) a census structure with spec :157's three components — a value census keyed by name recording resolved token or unreadable reason plus the binding's line, a module-level `poisoned` flag from an unresolved star import, and the set of form-5-eligible reference sites held BY NODE IDENTITY from `Module.body`; (b) the parse-loop build in `src/wardline/scanner/pipeline.py`, once per module, beside `build_import_alias_map`, applying the star-import poison predicate and the form-2 shadowed-root refusal at census-build time (spec :145, :159); (c) `SeedContext` gains the per-file census, `AnalysisContext` gains `Mapping[str, ModuleCensus]` following the `module_bindings` default-factory precedent (spec :158). Add `src/wardline/scanner/taint/provider.py`, `src/wardline/scanner/context.py` and `src/wardline/scanner/pipeline.py` to the owning Files lists. Add a test that a `def` nested in a module-level `if` is absent from the reference-site set (spec :131 — the case spec :160 calls the newest and least obvious).

### B6. Task 4 freezes `_match` at two changes and a two-tuple verdict, so the residual pair has nowhere to go and the reference site cannot reach the reader

**Plan:** 988, 1025-1034

**Why blocking:** Line 988 reads "there are exactly TWO changes ... `_match` keeps its existing `(FunctionTaint | None, str | None)` two-tuple return and every one of its existing return sites; no new verdict channel is added." Verified in source: `_match` (`decorator_provider.py:363-368`) receives no entity node — `taint_for` holds `entity` and passes only the decorator down — and its docstring states "Builtins return ``(None, None)`` here to stay silent (oracle-preserving)". `_match` is the only site holding both the argument name (`la.arg_name`) and the value node, i.e. the `(argument name, value text)` pair spec :170 requires reach the emission loop. The pinned call block at :1025-1034 passes six arguments and cannot bind the re-cut signature; the only repair keeping it as written is the defaulted census spec :155 forbids by name. This line is NOT on the spec's supersession list — it is a plan-derived conflict of the class spec :204 says that list is a floor for.

**Fix:** Delete "exactly TWO changes" and "no new verdict channel is added". Widen `_match`'s verdict (third tuple element or a small result dataclass) to carry `unreadable_level_values: tuple[tuple[str, str], ...]`, pinning that a builtin's unreadable branch returns the residual pair in place of today's `(None, None)` while a custom `BoundaryType` returns the released `(None, canonical_name)` and never the residual (spec :105/:119). Thread `entity`/`entity.node` and the census from `taint_for` into `_match`. Re-cut the :1025-1034 call to pass `census=`, `reference_site=entity.node`, `shadowed_roots=` and the builtin discriminator. Note the collision this creates with plan :1047's "`taint_for` is **not** touched by this task ... Task 6 Step 4.2 remains the sole place `taint_for` changes" and its restatements at :15 and :4099 — re-cut those in the same pass or Task 4 and Task 6 both claim `taint_for`.

### B7. Plan line 1049's no-field / no-`pipeline.py` prohibition is expressly superseded and blocks the residual carrier

**Plan:** 1049

**Why blocking:** Written as an absolute prohibition — "no `SeedResult` field, no `FunctionSeed` field, no `pipeline.py` change" — inside Task 4 Step 3's implementation text. Spec :198 supersedes the no-field and no-`pipeline.py` clauses by name. Verified in source that both refusals are fatal: `SeedResult` (`provider.py:54-73`) carries only `taint` and `unprovable_boundaries`, and its docstring's "Builtin boundary types NEVER appear here ... preserving the byte-identity oracle" is precisely why spec :170 bars reusing it as the carrier; `pipeline.py:166` is where the census must be built and beside which the FACT emission loop must sit.

**Fix:** Re-cut to: "There is no new *serialised* state: the residual carrier is a new, unserialised `SeedResult`/`FunctionSeed` field distinct from `unprovable_boundaries`, and `pipeline.py` gains the per-module census build and the residual-FACT emission loop — but `FunctionSummary`'s structural shape is unchanged, so there is no `SUMMARY_SCHEMA_VERSION` bump." Spec :198 and :208 explicitly PRESERVE the no-bump clause and plan :1051's restatement; carry both forward verbatim so the reviser does not delete them alongside the superseded clauses. The `sp1g`→`sp1h` `_RESOLVER_VERSION` sentence is likewise unaffected and still governs (spec :184).

### B8. No task owns the residual FACT's carrier, emission loop, fingerprint, or any of its five soundness conditions

**Plan:** 1601 (Task 6 Produces); new task before Task 7

**Why blocking:** `grep -i UNREADABLE-MARKER-VALUE` over all 4099 lines returns zero. `pipeline.py` is named as modified by Task 6 alone (:1596, :1859, :1876), and Task 6's `Produces:` at :1601 is a closed enumeration naming one new seed-field pair and one FACT id. Plan :37 independently reserves "new FACT id is **exactly** `WLN-ENGINE-UNKNOWN-MARKER`". So rev 6's second S0 deliverable (spec :470) has no field, no emission loop, no fingerprint construction and no test. Without it form 5 could ship its reader while everything that stays unreadable remains silent — the exact "neither substitutes for the other" split PDR-0018 forbids (spec :109). Verified separately: `pipeline._fp` is a positional NUL-join sha256, which does NOT produce spec :167's preimage (argument name + `ast.unparse` of the value node, NFC-normalised, truncated to 200 characters).

**Fix:** Add a task owning: `SeedResult.unreadable_level_values: tuple[tuple[str, str], ...] = ()` and the identical field on `FunctionSeed`, both unserialised; a second emission loop in `pipeline.py` beside the `unprovable_boundaries` loop emitting `rule_id="WLN-ENGINE-UNREADABLE-MARKER-VALUE"`, `Severity.NONE`, `Kind.FACT`; and an explicit statement of the fingerprint preimage per spec :167, stating whether `pipeline._fp` is reused (it does not currently satisfy the preimage) or a new helper is required. Give EACH of the five soundness conditions a named test and an owner, because none is currently owned and none fails loudly: (1) not voided by the `untrusted_sources` rebuild; (2) not inertness-clearing — `summary.total` unmoved, no provider-seeded row, scan stays inert; (3) NOT SUPPRESSIBLE, carrying §11.2's baseline posture of compared-and-reported; (4) the distinct fingerprint above; (5) counted in `decorator_coverage`.

### B9. Task 6's three-arm `taint_for` branch makes the residual population structurally unreachable, and forbids the fourth arm

**Plan:** 1841-1856, 1859-1873

**Why blocking:** Verified end to end. An unreadable builtin LEVEL value returns `(None, None)` from `_match` (source docstring: "Builtins return ``(None, None)`` here to stay silent"; plan :1039-1040 keeps `return None, (None if bt.builtin else bt.canonical_name)`), lands in the plan's `else` arm, and `unknown_vocabulary_marker` returns `None` there because `wardline.decorators.trusted` IS an exact `REGISTRY` export. The value is collected by no arm at all, and "Nothing else in `taint_for` changes" forbids adding one — while `taint_for` is the only place `SeedResult` is constructed (:320, :335), i.e. the only place the carrier can be populated. Separately, Step 4.4's replacement `FunctionSeed(...)` block for the `untrusted_sources` override is a closed enumeration with a frozen comment naming exactly two channels; verified at `pipeline.py:170-180` that the branch is a wholesale reconstruction erasing any field not named, so soundness condition 1 fails on precisely the population under the most explicit operator scrutiny.

**Fix:** Delete "Nothing else in `taint_for` changes". Add a fourth branch arm for a builtin marker whose `ArgKind.LEVEL` value the shared reader cannot read, appending the `(argument name, ast.unparse(value))` pair to the new list, and add the field to BOTH `SeedResult(...)` constructions (:320, :335). State that a custom `BoundaryType`'s unreadable level value stays on the `unprov` arm and never enters the list (spec :119). In Step 4.4, add the third channel to the replacement `FunctionSeed(...)` block and to its frozen comment, and add a third override-preservation test beside `test_config_source_override_preserves_unknown_markers` and `..._unprovable_boundaries` (plan :1735-1787), which today pin only two channels so nothing reds when the third is dropped.

### B10. The drop-coverage matrix reds on the fix, freezes the silence as a passing contract, and cannot see a Severity.NONE FACT

**Plan:** 2596, 2604-2614, 2628, 2657, 2674, 2677, 2679

**Why blocking:** Four compounding defects in one committed file. (a) Row :2657 `("unreadable_value", "@trusted(level=CFG)", "silent", True)` satisfies every form-5 precondition once the :2626-2630 preamble is reconstructed — named (not star) import, single unconditional direct-top-level `str` binding lexically preceding a `def` that is a direct element of `Module.body`, builtin marker, `ASSURED` in `allowed` — so under rev 6 it RESOLVES and the assertion at :2673 reds, with plan :27 and :42's stop discipline pointing the implementer at reverting the fix rather than amending the row. (b) The row as written freezes the false green into a shipped passing contract. (c) :2674 filters `fired` to `Kind.DEFECT`, so a `Severity.NONE` FACT is invisible before any assertion sees it — a false green inside the guard that exists to prove there are none. (d) :2677's XOR evaluates `False != False` for any FACT-only row, so the minimal two-line fix spec :202 lists reds on first execution and invites an implementer to weaken the never-two-channels exclusivity property. The Interfaces header at :2596 and the docstring at :2604-2614 (dictated into the committed file) both teach the superseded three-channel taxonomy, and :2596 and :2677 are NOT on the spec's supersession list.

**Fix:** Rewrite `tests/grammar/test_drop_coverage_matrix.py` in one pass. :2596 — four channels: PY-WL-130 (shape), PY-WL-114 (readable-but-invalid value), `WLN-ENGINE-UNREADABLE-MARKER-VALUE`, and ONE remaining deliberate silence, the shadowed builtin vocabulary root where no builtin LEVEL slot exists at all; state that a statically-unreadable builtin LEVEL value is never silent. :2604-2614 — rewrite the docstring accordingly and rename `test_shadowed_root_is_the_other_deliberate_silence` to `..._the_only_...` (its assertions stay sound). :2628 — add one explicitly-refused binding (e.g. `DYN = get_level()`, a call right-hand side, spec :142) and comment that `CFG` now RESOLVES. :2657 — change the row to expect a resolved seed with `drops=False`, and add a new row for `DYN` asserting the FACT channel is PRESENT. :2674 — collect over ALL kinds. :2677/:2679 — restructure to be channel-name-driven: every row names a non-empty channel and asserts that channel's finding is present, with exclusivity over the full marker channel set; retain a pure-absence branch only for rows whose channel is literally "none". Delete the "silent" channel value from the vocabulary for builtin-LEVEL rows. BLOCKED ON spec condition S2 (shape-gate/value-reader ordering) — fifteen of the 21 rows are shape offences that never reach the reader, and "every row asserts a present channel" cannot be written until that ruling exists.

### B11. No task touches the Rust frontend, so PRD-0003 criterion 6 is unmet by construction

**Plan:** new task; scope statements at 23, 106, 4092, 4098

**Why blocking:** Verified: zero references anywhere in the plan to `src/wardline/rust/*`; the only Rust content is :23's cargo-toolchain note and the scope statements putting `wardline-b857b50b54` outside S0. Spec :220 places the fix in S0 and states criterion 6 "is unmet regardless of the Python result if the Rust frontend is left out"; spec :470 lists it as an S0 deliverable. The design owes nothing further — spec :222-230 supplies the recogniser, the three-item refusal set, the upper bound, the existing `WLN-ENGINE-RUST-INVALID-TRUST-MARKER` channel and the re-confirmation obligation. Only the plan step is missing. (Note plan :23 is about the cargo toolchain and does not itself forbid the work — wardline's Rust frontend is Python — so the governing sites are the scope statements.)

**Fix:** Add an S0 task implementing spec §4.4's two-stage recognise-then-parse split in `src/wardline/rust/provider.py`, its `ValueError` flowing through the entity loop's existing `except ValueError` into `_invalid_trust_marker_finding`. Pin the three refusals as named tests: the start-anchored `match` (never `search`), the exact `@trusted` token, and the required parenthesised argument list. Re-confirm the 21-canonical-marker zero-blast-radius measurement at implementation (spec :230). Place the criterion-1 exit-code repro in `tmp_path` and in none of `tests/corpus/rust/**`, `tests/golden/identity/rust/corpus/*.json`, `tests/golden/identity/rust/fixtures/**` (spec :189). Update :23's Tech Stack line.

## Conditions for plan rev 3.5 (25)

### C1. [BLOCKING] S0 stage scope and close criteria — fold in both rev-6 siblings

**Target:** Filigree discipline :106; Final verification :4092; Self-review notes :4098 (and the tail of :107)

Remove `wardline-b857b50b54` and `wardline-2b2a6cddfa` from the "out of S0 scope" / "Deliberately NOT in S0" lists at :106 and :4098 and from the residue framing at :4092, replacing them with an in-scope statement citing spec §13.2's S0 row. PRESERVE the rev-3.3 honesty discipline verbatim: `wardline-4928b75782` still closes after T6 as "the call-shape half is fixed", never as "the false green is fixed", still naming both siblings — they are genuinely still open at that moment. Add to `wardline-5a795253f1`'s close criteria: both siblings CLOSED with committed exit-code regression tests (spec :188); the residual FACT's five soundness conditions each pinned by a named test (spec :164-168); and the blast-radius re-run showing zero delta (spec :187). Retain the genuinely-deferred items in the :4098 list unchanged. GATED ON owner decision 2 — if the owner rules the siblings into a later stage instead, this condition inverts into an amendment of spec §13.2 rather than of the plan.

### C2. [BLOCKING] Delete the "do not re-open" prohibition on the hole-3 fix

**Target:** Filigree discipline :107

Delete the "Settled in rev 3.4 — do not re-open" heading, the two-gap T12 matrix scoping, and the closing "`wardline-2b2a6cddfa` stays open for both". Replace with a "Re-opened by PDR-0018 / spec rev 6 §4.2.1" bullet recording that the ticket is re-opened by owner decision and closed inside S0 by form 5 plus `WLN-ENGINE-UNREADABLE-MARKER-VALUE`. PRESERVE the one clause spec :199 calls "unchanged and reinforced": an unreadable level value must never appear in any rule's `examples_clean`.

### C3. [BLOCKING] Re-cut the shared reader's frozen produced interface

**Target:** Task 2 Produces list :385 and :388

Add to BOTH `level_token` and `read_level` as required keyword parameters with no default: the per-module census and the reference site (the decorated `def`/`async def` statement, passed directly by each caller), per spec :155; plus `shadowed_roots` per spec :159, noting that the form-5 shadowed-root refusal is applied at the census build and must not be re-derived at the value's use site. Retain line 385's "STRICT: alias-resolved `wardline.core.taints.TaintState` receiver or str literal" — spec :154 and :200 explicitly do not supersede it. State a mechanism that keeps form 5 builtin-only (spec :105/:119): either a required non-defaulted discriminator on both entry points or an explicit caller-side gate stated in Task 4; plan :1028's `bt.builtin` ternary feeds only the `declared=` set and is NOT such a gate. State how the caller distinguishes spec :121's UNREADABLE+FACT from spec :144's READS-then-rejects and how the `(argument name, ast.unparse(value node))` pair reaches the emission loop — a discriminated return type is one solution, composing the already-produced `extract_keywords` with `level_token` is another; the plan must name one, and must pin that a token read then rejected by the `allowed` check never emits the residual FACT. Pin spec :156's three-way disposition: RESOLVES; `None` when the census is present but holds no qualifying entry or the reference site is ineligible; RAISE when no census is present for the module at all (rule side lands on per-rule isolation as `WLN-ENGINE-RULE-FAILED` ERROR DEFECT, provider side propagates out of the parse pass). Choose ONE name for the census type and use it consistently.

### C4. [BLOCKING] Widen P9's CASES table to revision 6's eight cases on the real construction path

**Target:** Task 2 Step 5, CASES table :697-715 (row at :705) and the agreement tests at :766-801

Executed in the SAME pass as condition 3 — spec :444 makes the interface amendment and the table widening one change. Replace row :705 `("LEVEL", {}, None)` and widen the table to hold at minimum spec :160's eight cases: bound (resolving); unbound; method-or-nested-`def`/`async def` reference site; conditionally-defined module-level `def`/`async def` reference site; two-occurrence census; `global`-declared name; star-import-poisoned module; custom `BoundaryType` LEVEL argument (asserting `WLN-ENGINE-UNPROVABLE-BOUNDARY` + `UNKNOWN_RAW`, never the residual FACT). Drive the six census/reference-site cases through `run_scan(tmp_path)` on the analyser's real construction path, each asserting on BOTH the rule-side finding set and `result.context.declared_qualnames` so a one-sided plumbing gap reds; state explicitly that a census literal hand-built inside a parametrised test does not satisfy P9 (spec :156). Add a `pytest.raises` row for the no-census plumbing defect — plan :503-504's existing `raise ValueError` for an undeclared arg is the in-module precedent. Retain the expression-level table only as a supplementary unit check taking an explicit census and reference site. Also re-cut the now-uncompilable call in `test_read_level_accepts_sibling_declared_keywords` at :826-839 (drop any claim it exercises the custom path — both its values are `str` literals, so form 5 never engages).

### C5. [BLOCKING] Add a task owning the per-module census type, its parse-loop builder, and both context carriers

**Target:** New task, inserted after Task 2 and before Task 4; Files lists at :369-376 and :880-886

PLANNING ACT, not an edit. Create a task owning: (a) a census structure with spec :157's three components — a value census keyed by name recording the resolved token or an unreadable reason together with the binding's line; a module-level `poisoned` flag set by an unresolved star import; and the set of form-5-eligible reference sites (the `def`/`async def` statements that are direct elements of `Module.body`) held BY NODE IDENTITY, sound because module source is parsed exactly once and both sides receive the same node objects. (b) The build in `src/wardline/scanner/pipeline.py`, once per module inside the parse loop beside `build_import_alias_map` and before the `SeedContext(...)` construction at :166, applying the star-import poison predicate (spec :145) and the form-2 right-hand-side shadowed-root refusal (spec :159) at build time. (c) `SeedContext` gains the per-file census; `AnalysisContext` gains a module-path→census mapping following the shipped `module_bindings: Mapping[str, SinkBindings] = field(default_factory=dict)` precedent at `context.py:122` (spec :158) — the census value is built once and handed to both sides, never rebuilt. Add `src/wardline/scanner/taint/provider.py`, `src/wardline/scanner/context.py` and `src/wardline/scanner/pipeline.py` to the owning Files list. Add a test that a `def` nested in a module-level `if` is absent from the reference-site set (spec :131). Note that spec :198 supersedes plan :1049's `pipeline.py` clause only for the FACT emission loop, so the census build needs its own explicit extension of that supersession.

### C6. [BLOCKING] Widen `_match`'s verdict and re-cut Task 4's frozen `_read_level` call site

**Target:** Task 4 Step 3 :988 and the call block :1025-1034; consequential edits at :1047, :15, :4099

Delete "there are exactly TWO changes", "`_match` keeps its existing `(FunctionTaint | None, str | None)` two-tuple return and every one of its existing return sites", and "no new verdict channel is added". Widen `_match`'s verdict — a third tuple element or a small result dataclass — to carry `unreadable_level_values: tuple[tuple[str, str], ...]`, since `_match` is the only site holding both the argument name and the value node that spec :170 requires reach the emission loop. Pin that a BUILTIN's unreadable branch returns the residual pair in place of today's `(None, None)` while a CUSTOM `BoundaryType` returns the released `(None, canonical_name)` and never the residual (spec :105/:119). Thread `entity`/`entity.node` and the census down from `taint_for` into `_match`. Re-cut the :1025-1034 call to pass `census=`, `reference_site=entity.node`, `shadowed_roots=` and the builtin discriminator, and state explicitly that neither the census nor the reference site may carry a default. Re-scope plan :1047's "`taint_for` is **not** touched by this task ... Task 6 Step 4.2 remains the sole place `taint_for` changes" and its restatements at :15 and :4099, or Task 4 and Task 6 will both claim `taint_for`.

### C7. [BLOCKING] Re-cut the no-field / no-`pipeline.py` prohibition, preserving the no-schema-bump clause

**Target:** Task 4 Step 3 :1049 (and the restatement at :1051)

Re-cut :1049 to: "There is no new *serialised* state: the residual carrier is a new, unserialised `SeedResult`/`FunctionSeed` field distinct from `unprovable_boundaries`, and `pipeline.py` gains the per-module census build and the residual-FACT emission loop — but `FunctionSummary`'s structural shape is unchanged, so there is no `SUMMARY_SCHEMA_VERSION` bump (spec §4.3). PY-WL-130 re-derives the malformed-shape verdict from the AST." Spec :198 and :208 explicitly PRESERVE the no-bump clause and plan :1051's restatement — carry both forward verbatim so the reviser does not delete them alongside the superseded clauses. The `sp1g`→`sp1h` `_RESOLVER_VERSION` sentence is likewise unaffected and still governs (spec :184: `_RESOLVER_VERSION` is the only one of the four markers rev 6 moves).

### C8. [BLOCKING] Add a task owning the residual FACT's carrier, emission, fingerprint, and all five soundness conditions

**Target:** New task, after the census task and before Task 7; extends Task 6's Produces list at :1601

PLANNING ACT, not an edit. Create/extend a task producing: `SeedResult.unreadable_level_values: tuple[tuple[str, str], ...] = ()` and the identical field on `FunctionSeed`, both unserialised; and a second emission loop in `src/wardline/scanner/pipeline.py` beside the `unprovable_boundaries` loop, emitting `rule_id="WLN-ENGINE-UNREADABLE-MARKER-VALUE"`, `Severity.NONE`, `Kind.FACT`. State the fingerprint preimage explicitly per spec :167 — argument name plus `ast.unparse` of the value node, NFC-normalised then truncated to 200 characters — and state whether `pipeline._fp` is reused; it is a positional NUL-join sha256 and does NOT currently produce that preimage, so either a new helper is required or the plan must show `_fp` satisfies it. Give EACH of the five soundness conditions a named test and a named owner, because none is currently owned and none fails loudly: (1) not voided by the `untrusted_sources` rebuild; (2) not inertness-clearing — assert `summary.total` unmoved, no provider-seeded row created, and `compute_resolution_posture(...).inert is True` still holds (extend the existing assertion at plan :1790); (3) NOT SUPPRESSIBLE, carrying §11.2's baseline posture of compared-and-reported — this condition is currently unowned in every task and is the one no test would catch; (4) the distinct fingerprint above; (5) counted in `decorator_coverage` per condition 15. GATED ON spec condition S3 for the count's shape and S4 for the fingerprint.

### C9. [BLOCKING] Add the fourth `taint_for` arm and the third override-preserved channel

**Target:** Task 6 Step 4.2 :1841-1856 and Step 4.4 :1859-1873, plus the tests at :1735-1787

Delete "Nothing else in `taint_for` changes". Add a fourth branch arm — after the unknown-marker probe — for a BUILTIN marker whose `ArgKind.LEVEL` value the shared reader cannot read, appending the `(argument name, ast.unparse(value))` pair to the new list, and add the field to BOTH `SeedResult(...)` constructions at :320 and :335. State explicitly that a CUSTOM `BoundaryType`'s unreadable level value stays on the `unprov` arm and never enters the list (spec :119, and condition 5's no-double-count clause). In Step 4.4, add the third channel to the replacement `FunctionSeed(...)` block and extend its frozen comment to name all three (unprovable custom boundary / unknown marker / unreadable builtin LEVEL value) — verified at `pipeline.py:170-180` that the branch is a wholesale reconstruction erasing any field not named. Add a third override-preservation test beside `test_config_source_override_preserves_unknown_markers` and `..._unprovable_boundaries`, which today pin only two channels so nothing reds when the third is dropped.

### C10. [BLOCKING] Rewrite the drop-coverage matrix — four sites, one pass

**Target:** Task 12: Interfaces :2596, docstring :2604-2614, preamble :2628, row :2657, assertions :2674 / :2677 / :2679

One rewrite of `tests/grammar/test_drop_coverage_matrix.py`. :2596 — replace the three-channel taxonomy with four: PY-WL-130 (call shape), PY-WL-114 (readable-but-invalid value), `WLN-ENGINE-UNREADABLE-MARKER-VALUE` (`Severity.NONE` FACT, every builtin LEVEL value that stays unreadable after form 5), and the ONE remaining pinned deliberate silence — a shadowed builtin vocabulary root, which disables the marker candidate so no builtin LEVEL slot exists; state that a statically-unreadable builtin LEVEL value is NEVER silent. :2604-2614 — rewrite the docstring to match and delete the tracked-false-green paragraph; rename `test_shadowed_root_is_the_other_deliberate_silence` to `..._the_only_deliberate_silence` (its assertions remain sound). :2628 — add one explicitly-refused binding, e.g. `DYN = get_level()` (a call right-hand side, spec :142), with a comment recording that `CFG` now RESOLVES and `DYN` does not. :2657 — change the `unreadable_value` row to a resolved seed with `drops=False`, and add a new row for `DYN` naming the FACT channel. :2674 — collect `fired` over ALL kinds (drop `if f.kind is Kind.DEFECT`). :2677/:2679 — restructure channel-name-driven: every row names a non-empty channel and asserts that channel's finding is PRESENT, with exclusivity asserted over the full marker channel set (a minimal fix that only adds the FACT id to the existing two-id `if` makes the XOR evaluate `False != False` and reds on first run); retain a pure-absence branch only for rows whose channel is literally "none". Remove the "silent" channel value from the builtin-LEVEL vocabulary. BLOCKED ON spec condition S2.

### C11. [BLOCKING] Add a task implementing the §4.4 Rust recognise-then-parse split

**Target:** New S0 task; Tech Stack line :23

PLANNING ACT, not an edit. Add a task implementing spec §4.4's two-stage recognise-then-parse split in `src/wardline/rust/provider.py`, confining the widening to the parenthesised argument list, whose contents are captured verbatim up to the FIRST closing parenthesis and not interpreted. Route the parse failure's `ValueError` through the entity loop's existing `except ValueError` into `_invalid_trust_marker_finding` / `WLN-ENGINE-RUST-INVALID-TRUST-MARKER`; mint no new rule id and widen no vocabulary. Pin spec :225's three refusals as named tests — the start-anchored `match` (never `search`), the exact `@trusted` token, and the requirement of a parenthesised argument list. Re-confirm the 21-canonical-marker zero-blast-radius measurement at implementation (spec :230). Place the criterion-1 exit-code repro in `tmp_path` and in none of `tests/corpus/rust/**`, `tests/golden/identity/rust/corpus/*.json` or `tests/golden/identity/rust/fixtures/**` (spec :189). Update the :23 Tech Stack line so it no longer reads as scoping Rust out. GATED ON owner decision 2.

### C12. [MAJOR] Re-pin every spec-revision site from rev 5 to rev 6 — nine sites, after the plan body absorbs rev 6

**Target:** Preamble blockquotes :11 and :15; Global Constraints :36; preflight `git hash-object` check :92-97; task-dependency line :101; Task 17 :3083 and its committed test comment :3106; Task 18 Step 2 :3579; Self-review record :4099

Re-cut all nine sites in one pass, and ONLY after the plan body has absorbed rev 6's supersessions — a re-pin done first silently blesses the unreconciled directives. Per plan :4099's own recorded lesson, grep the revision WORD ("rev 5", "revision 5", "rev-5") as well as the digest `9624f892…`; the rev-3.4 inventory built by grepping one token missed two of five sites. Demote blob `9624f8925a006a80677c12eaa0951933d631920f` to the review-provenance list beside `b43aab4b…`, `4956ba3b…` and `0f04eeb1…`, and pin the rev-6 blob DERIVED FROM THE COMMIT: `git hash-object` hashes the WORKING-TREE file, so re-pinning to the currently-uncommitted rev-6 file's hash would make the check pass while pinning a blob reachable from no commit, voiding the provenance the pin exists for. Retain the "any later spec drift is STOP-and-re-review" clause verbatim. At :3083 also correct the loose "§13.1.1" citation — the spec has no numbered §13.1.1, only §13.1's item 1 — and record that Task 17's engineering content is unchanged by rev 6 so the re-pin is not mistaken for a behavioural amendment. Note the reach honestly: the executable check gates Tasks 17/18/20 (:92, :101), not Tasks 1-16.

### C13. [MAJOR] Reserve the second FACT id

**Target:** Global Constraints :37

Re-cut to: "New rule id is exactly **`PY-WL-130`**; new FACT ids are exactly **`WLN-ENGINE-UNKNOWN-MARKER`** and **`WLN-ENGINE-UNREADABLE-MARKER-VALUE`** (both `Severity.NONE` + `Kind.FACT`, both reserved by the spec, the second added at rev 6 §4.2.1; the second is builtin-only and never co-emits with `WLN-ENGINE-UNPROVABLE-BOUNDARY` on the same site). Next free numeric RULE id after this plan is 131." Keep the numeric-rule-id clause distinct from the FACT ids, which are not numbered. This directive is absent from the spec's own supersession list.

### C14. [MAJOR] Widen the zero-golden-drift constraint to the Rust corpora, the value population, and repro placement

**Target:** Global Constraints :27

Add `tests/corpus/rust/**`, `tests/golden/identity/rust/corpus/*.json` and `tests/golden/identity/rust/fixtures/**` to the no-regeneration list — verified these frozen trees exist and carry `/// @trusted(level=ASSURED)` markers, and `tests/golden/identity/corpus/*.json` does not glob them. Replace the census sentence: the `to_level` census (2026-08-09) covers marker call SHAPE only, and rev 6 adds two emissions over a disjoint population (a form-5-resolved seed and the residual FACT on an unreadable value), so a separate value census is required and the blast-radius measurement must be re-run once the form-5 plumbing shape is fixed (spec :187). Add spec :189's placement rule explicitly: both rev-6 repros live in `tmp_path` unit or integration tests and in NONE of the trees named above, on either frontend — the plan currently states no placement rule, and a specimen committed into a fixture tree is silently absorbed, converting PRD-0003 criterion 4's guard into a re-freeze. Widen the "Tasks 4–6" enumeration to whatever tasks carry rev 6. (Independently measured during this audit: every builtin `level=`/`to_level=` value under the frozen Python fixture trees is a `str` literal, so the conclusion holds today — but not by the evidence the plan names.) Do NOT mirror this into Task 5 Step 6's criterion at :1581, which is about PY-WL-130, a call-shape rule, for which the shape census is the correct evidence.

### C15. [MAJOR] Add the seventh `decorator_coverage` summary key inside Task 7's already-sanctioned re-freeze

**Target:** Task 7 Produces :1931, Step 3's summary dict :1977-1984, the MCP schema edit :1994, and the re-freeze at :1997-2012

Produce SEVEN summary keys, not six. `DecoratorCoverageReport` gains a residual count beside `unknown_marker_count`; `decorator_coverage_from_scan` tallies findings whose `rule_id` is `WLN-ENGINE-UNREADABLE-MARKER-VALUE`; the summary dict gains the key; and `_DECORATOR_COVERAGE_OUTPUT_SCHEMA` gains both the property and the `required` entry — verified the shipped golden's `decorator_coverage.properties.summary` is `additionalProperties: false` with `required: [total, clean, defect, unknown, suppressed]`, so a later addition would fail schema validation. This rides Task 7's SINGLE already-sanctioned re-freeze with one `VENDORED_BLOB_SHA` bump; Global Constraint :28 caps the NUMBER of re-freezes, not the keys changed within one, so :28 needs NO amendment. Per plan :2016's own rule, extend the key through `cli/decorator_coverage.py::_render_human` and all four named test modules. Add a sibling to `test_summary_counts_unknown_markers` planting an unreadable builtin LEVEL value and asserting both the new count and — per soundness condition 2 — that `summary["total"]` is unmoved and no row is created. Name both FACT ids in Task 7's Interfaces "Consumes" line. GATED ON spec condition S3.

### C16. [MAJOR] Resolve Task 2 Step 2's retained private `_read_level` against the re-cut signature

**Target:** Task 2 Step 2 :628 (and the import at :622)

State how the Task 2 commit simultaneously satisfies three constraints the plan currently leaves in tension: the surviving private `_read_level` calls `_level_token(values[0], alias_map)` two-positionally (verified in `decorator_provider.py`) and must honour the re-cut signature; form-5 resolution must stay inert until Task 4's cache/version gate, since :628's own final clause forbids changing seeding in an intermediate commit; and no default may be added to the new parameters to make the legacy caller compile (spec :155 forbids the defaulted-empty census by name). Two viable options: forward an explicitly form-5-disabled census view, or move the `_read_level` deletion and the `sp1g`→`sp1h` bump into Task 2. Pick one and state it.

### C17. [MAJOR] Delete PY-WL-114's shipped unreadable-value clean exemplar and do not add a new one

**Target:** Task 2 Step 3 :630-658 (the `examples_clean` append at :652-658); Files list already names the file at :372

Two edits in the step that already owns this METADATA. (a) DELETE the existing third `examples_clean` entry in `src/wardline/scanner/rules/invalid_decorator_level.py` — verified live at line 55 as `"@trusted(level=cfg.LEVEL)\ndef h(p):\n    return p"` — outright, not re-annotated; spec :186 refuses the "clean *with* a FACT" reclassification by name. (b) DO NOT append the `myconfig.TaintState.ASURED` entry at :652-658: its receiver does not alias-resolve, so under spec :129 it is an unreadable builtin LEVEL value that takes the residual FACT, and shipping it as a clean exemplar re-forms the exact trap rev 3.4 removed one rule over. Pin the foreign-receiver silence property as a unit assertion in `tests/unit/scanner/rules/test_invalid_decorator_level.py` instead. Add to Task 5's suite an assertion that no rule's `examples_clean` snippet emits `WLN-ENGINE-UNREADABLE-MARKER-VALUE` — verified `tests/unit/scanner/rules/test_rule_examples_meta.py` filters to `Kind.DEFECT`, so a `Severity.NONE` FACT reds nothing and neither deletion is forced by a failing test. Add both METADATA edits to the task's explicit staged-path list.

### C18. [MAJOR] Split the `declared_qualnames` posture pin between malformed shape and form-5 resolution

**Target:** Task 4 :971 (comment at :970-972) and the standing instruction at :1047

Re-word the :971 comment to: "a lone marker with a malformed SHAPE must NOT enter `declared_qualnames`; a lone marker whose level RESOLVES — including via P3 form 5 — DOES enter it and counts in the anchored posture bucket (spec §4.2.1, §11.4)". The test itself (`test_malformed_marker_alone_still_takes_no_opinion`) survives rev 6 unchanged — a shape offence is not a value problem — so add a SIBLING test pinning that a module-top-level `_SVC_LEVEL = 'ASSURED'` with `@trusted(level=_SVC_LEVEL)` seeds and enters `declared_qualnames`. Explicitly scope :1047's "do not weaken any of them" to malformed-SHAPE assertions so it is not read as barring the form-5 addition; it names Task 12's matrix, one of whose rows must invert. Spec :183 and :201 name plan line 971 by number.

### C19. [MAJOR] Re-express the shape-vs-value partition test over a value that stays unreadable

**Target:** Task 5 :1237-1244 (`test_unreadable_value_is_not_a_shape_offence`)

The fixture `CFG = 'ASSURED'` with `@trusted(level=CFG)` on a module-top-level `def` satisfies form 5 in full and RESOLVES under rev 6; `assert not _hits(result)` still PASSES (a resolved valid token fires no PY-WL-130, and `_hits` filters to PY-WL-130), so this is a green test that silently stops exercising the property it names — the more dangerous shape. Re-express it over a value that stays unreadable (a call, an f-string, or a two-hop bare name), asserting both `not _hits(result)` (no shape offence) AND that `WLN-ENGINE-UNREADABLE-MARKER-VALUE` is PRESENT and the qualname is absent from `declared_qualnames`. Correct the inline comment, which is otherwise the last surviving statement of the pre-rev-6 model inside the PY-WL-130 suite. Add a separately-named companion test pinning that `CFG = 'ASSURED'` + `@trusted(level=CFG)` resolves: no PY-WL-130, no residual FACT, qualname present in `declared_qualnames`. Spec :197 names plan lines 1237-1242.

### C20. [MAJOR] Extend the Changelog with rev 6's three user-visible changes

**Target:** Task 16 Step 1/Step 2, `### Added` block :3006-3028 ("Companion FACT" sentence at :3020, coverage key at :3023)

Task 16 is the plan's only `CHANGELOG.md` owner and its note currently describes the increment as a call-shape fix. Add three bullets: (a) P3 form 5 — a same-file, module-level, one-hop value reference is now read in a builtin marker's LEVEL slot on a module-top-level `def`/`async def`, with the reference-site restriction and the one-hop, single-binding, lexical-precedence, no-`global`, no-star-import discipline stated, plus an explicit gate-colour note that previously-silent functions now enter the declared set and their tier-modulated DEFECTs fire (classified TP by construction, spec :185) — this is the single most consequential user-facing change in the increment and is currently unrecorded; (b) `WLN-ENGINE-UNREADABLE-MARKER-VALUE` — `Severity.NONE` FACT, builtin-only, non-suppressible, counted in `decorator_coverage`; amend the "Companion FACT" sentence at :3020 to name both FACTs and both coverage counters; (c) the Rust recogniser widening routed through the existing `WLN-ENGINE-RUST-INVALID-TRUST-MARKER`, no new rule id, no vocabulary widening (contingent on condition 11). Step 3's `sp1g`→`sp1h` version-discipline record needs NO change — verified correct against spec :184 and :208.

### C21. [MAJOR] Add the blast-radius re-run with the blinded negative control re-confirmed live

**Target:** Final verification, new bullet after :4083

A whole-plan search for "blast radius", "negative control" and "blinded" returns only :13's passing note about the Rust sibling — nothing requires a re-run. Add: "Blast-radius re-run (spec §4.2.1): with the form-5 plumbing in place, re-run the same four frozen-oracle suites and re-confirm the blinded negative control LIVE in the same session; record both results. Any delta is PRD-0003 criterion 4's reject branch — stop and fix the change; it is NOT covered by §12's one-rekey / one-`regen.py --reason` per-kind allowance, which revision 6 does not consume because it introduces no new declaration kind." The control is what makes an "all same" result meaningful rather than a no-op; "full suite green" cannot distinguish "form 5 moved nothing" from "the control stopped discriminating". Note that Global Constraint :27 already forbids re-freezing a red scan golden, so the gap is the missing positive obligation, not the reject discipline.

### C22. [MAJOR] Add the two rev-6 exit-code repros to Final verification with their placement rule

**Target:** Final verification, new bullets after :4085

Spec :188 obliges these and states no other spec text does. Add: "End-to-end repro (`wardline-2b2a6cddfa`): a `tmp_path` project whose only defect is `@trusted(level=_SVC_LEVEL)` with `_SVC_LEVEL = 'INTEGRAL'` at module top level on a module-top-level `def` exits 1 at `--fail-on ERROR` (was 0); pinned by a committed test asserting the EXIT CODE" and "End-to-end repro (`wardline-b857b50b54`): a `tmp_path` Rust project with a non-canonical `/// @trusted(...)` marker exits 1 at `--fail-on ERROR` via `WLN-ENGINE-RUST-INVALID-TRUST-MARKER` (was 0); pinned by a committed test asserting the EXIT CODE." State in both that neither specimen may be committed to `tests/corpus/fixtures`, the identity corpus, or the frozen Rust fixture tree. While here, consider adding the same committed exit-code artifact for hole 1 — see audit caveats.

### C23. [MAJOR] Widen the Goal, Architecture paragraph and coverage map to rev 6's S0 content

**Target:** Goal :17, Architecture :21, Self-review coverage map :4096

Bookkeeping so the plan's acceptance statement and completeness audit stop contradicting its tasks. :17 — add P3 form 5 with its per-module census (bindings AND form-5-eligible reference sites), the residual `WLN-ENGINE-UNREADABLE-MARKER-VALUE` FACT with its five soundness conditions, and the Rust recognise-then-parse split; state the governing spec revision as 6 in the same sentence; note that P9 is closed only when exercised over the eight form-5 cases. :21 — add the census's transport (`SeedContext` / `AnalysisContext`) and the second FACT channel to the data-flow summary. :4096 — add owners for spec §4.2.1, §4.4 and rev 6's P9 amendment; the current "P9 → Task 2" entry is false as long as Task 2's CASES table can express at most two of the eight required rows. Do not present the coverage map as complete until those owners exist.

### C24. [MINOR] Correct the shipped engine-floor and rule docstrings that state the superseded premise

**Target:** Task 2 module docstring :404-405 and `call_shape_offences` docstring :550-554; Task 2 test comment :753-754; Task 5 module docstring :1377-1379; Task 5 rationale :1564

These are dictated verbatim into committed source, so they ship. Spec :204 classifies them as stale text for the plan's own revision pass, but they must not survive: a shipped engine-floor docstring teaching that an unreadable level value is "the reader's documented no-opinion" will be cited as authority for leaving the residual channel unbuilt. State in each that a bare `Name` satisfying form 5 RESOLVES; that a value which stays unreadable takes `WLN-ENGINE-UNREADABLE-MARKER-VALUE`, never silence; and that a readable-but-invalid token stays PY-WL-114's DEFECT and takes no FACT. Name the no-census raise in the module docstring. Change the :753 comment to a value that stays unreadable (the ASSERTION at :754 survives unchanged — `call_shape_offences` reads no values). At :1564, re-cut the parenthetical premise (`_SVC_LEVEL` now resolves; `cfg.LEVEL` is observable on a non-gating channel that `test_rule_examples_meta.py`'s defect filter would not catch) and replace "leaves the fix open" with a statement that the fix lands in this stage; the INSTRUCTION at :1564 is unchanged and reinforced (spec :199). Do NOT change plan :1089's "where it stays silent (... unreadable VALUES ...)" — that describes where PY-WL-130 stays silent, which remains true.

### C25. [MINOR] Extend the self-scan gate's stated meaning and the two grammar tests' comments

**Target:** Final verification :4084; additive edits to `tests/grammar/test_unprovable_boundary.py` and `tests/grammar/test_provider_loop.py`

:4084 names PY-WL-130 as the only newly-firing channel. Extend: "If PY-WL-130, PY-WL-114, or any tier-modulated DEFECT newly fires, it is a REAL defect (spec §4.2.1 classifies both newly-firing populations TP by construction) — fix the source, never suppress. Exit 0 says nothing about `WLN-ENGINE-UNREADABLE-MARKER-VALUE`, which is `Severity.NONE` and never gates: verify that population separately via `decorator_coverage`, and confirm it carries no waiver (soundness condition 3: never suppressible)." This is an incompleteness in the gate's stated meaning, not a predicted red — spec §14 measures zero bare-`Name` level values repo-wide. Separately, `test_unprovable_builtin_emits_no_fact` and `test_unprovable_builtin_does_not_signal` must be amended ADDITIVELY: both existing absence assertions are things rev 6 REQUIRES (spec :105/:119 — a builtin unreadable LEVEL value never takes `WLN-ENGINE-UNPROVABLE-BOUNDARY` and never appears in `unprovable_boundaries`), so keep them, correct the "stays silent (no FACT)" comments, and add an assertion on the new channel. Assign these to whichever task owns the residual FACT, not to Task 4.

## Sequence to a legal execution start

1. **[HUMAN]** Commit spec revision 6. It is uncommitted in the working tree, so its blob is reachable from no commit and no honest pin exists. This is the gate on everything downstream and PDR-0018 states the ordering itself: "rev 6 → plan re-pin → preflight → execute".
2. **[HUMAN]** Rule owner decision 2 — where the two siblings land. Spec :470 places `wardline-2b2a6cddfa` and `wardline-b857b50b54` inside S0 under ticket `wardline-5a795253f1`; PDR-0017 explicitly REJECTED the alternative "Hold S0 until the spec rev 6 amendment lands" and its Decision is scoped verbatim to `wardline-4928b75782`. This determines whether rev 3.5 folds the two siblings into S0 or whether spec §13.2 is amended to open new stages. Conditions 1 and 11 invert on the answer. Can run in parallel with step 1.
3. **[AGENT]** Resolve the four spec conditions before the plan revision starts: rule the shape-gate/value-reader ordering (S2 — Task 4 inserts `call_shape_offences` before the levels loop, so a marker with both a malformed shape and an unreadable value never reaches the reader, which Task 12's rewrite cannot be written around); name the `decorator_coverage` summary key explicitly (S3 — Task 7 carries one of exactly two sanctioned golden re-freezes, so the shape must be right the first time); state whether `pipeline._fp`'s positional NUL-join sha256 satisfies spec :167's NFC-normalised, 200-char-truncated preimage or a new helper is required (S4); and check `diagnostics._BUILTIN_MARKER_IMPORTS` against spec :213's extension checklist for any rev-6 obligation.
4. **[HUMAN]** Rule or explicitly defer owner decision 1 — the G2 narrowing (spec :174-179). It changes only criterion 2's verdict and blocks no engineering (spec :174: every other obligation stands whichever way it is ruled), but the close receipt cannot be adjudicated without it: spec :179 requires three artifacts to put a ratification in force, and absent all three the spec treats the narrowing as not in force. Can run in parallel with steps 3 and 5.
5. **[AGENT]** Derive the rev-6 blob hash FROM THE COMMIT created in step 1. Strictly dependent on step 1: `git hash-object` hashes the working-tree file, so re-pinning against a dirty tree would make the preflight check pass while pinning a blob reachable from no commit — voiding the provenance the pin exists for.
6. **[AGENT]** Write plan revision 3.5, applying conditions 1-25 in order. This is not a re-pin pass: it adds three new tasks (census type/builder/carriers; residual FACT carrier/emission/five soundness conditions; the §4.4 Rust split) and re-cuts Task 2's frozen produced interface, which is S0's engine core. Per spec :204 and plan :4099, grep the PREMISE — the unreadable-level-value silence, the `wardline-2b2a6cddfa` residue framing, the no-new-seed-field framing, the two-gap matrix framing — not only the line numbers, and treat the spec's supersession list as a floor. Apply the blob re-pin (condition 12) LAST, after the body has absorbed rev 6, so the re-pin does not silently bless unreconciled directives.
7. **[AGENT]** Re-review plan rev 3.5. Plan :36 already places the current state in its own STOP-and-re-review condition, and the re-cut touches S0's engine core rather than its residue, so the prior three-panel green does not carry over.
8. **[HUMAN]** Refresh the product workspace: update roadmap.md's Now anchors, which still cite the spec at `ed7bfe86` and the rev-3 plan at `5252e3f5` — three spec revisions and two plan revisions stale — and record the dated metrics.md G2 reading. The series' latest entry is 2026-08-09 (PDR-0012) at 1 known false-green hole while PRD-0003 :30 baselines at 3, so criterion 2 would currently be read against a value never written down. PDR-0017 names this reading as a reversal-trigger crossing.
9. **[AGENT]** Re-run the live four-repo clean-target preflight and the re-pinned blob check, then begin execution at Task 1.

## Owner decisions outstanding

- **Where do `wardline-2b2a6cddfa` and `wardline-b857b50b54` land — inside S0 as spec §13.2 :470 states, or in a later stage as plan :106/:4098 state and as PDR-0017's rejected alternative implies?**
  - What turns on it: The shape of the entire rev-3.5 revision. Spec :8 says the spec governs where the two conflict, which folds both into S0 and makes conditions 1, 5, 8 and 11 mandatory plan additions. But PDR-0017's Decision reads "S0 executes as-is against plan rev 3.4 for `wardline-4928b75782`" and explicitly REJECTS "Hold S0 until the spec rev 6 amendment lands" — so the spec has unilaterally moved two deliverables into a stage the owner scoped otherwise. Note the two siblings have different authority chains: PDR-0018 re-opens `wardline-2b2a6cddfa` on owner authority, whereas `wardline-b857b50b54` enters S0 on spec §4.4's engineering grounds alone with no PDR behind it. If the answer is "later stage", spec §13.2 must be amended and rev 3.5 shrinks to conditions 2-4, 6-7, 10, 12-25; either way the two documents cannot both stand.
- **Is the G2 narrowing ratified — does G2 count only *silent* fail-open, so an observable-but-seed-dropped residual is not an open hole for criterion 2?**
  - What turns on it: Only criterion 2's verdict. Ratified, the permanent residual population (calls, f-strings, runtime config lookups — which PRD :92-94 puts outside this bet by design) stops counting and criterion 2 becomes satisfiable at 0. Rejected, criterion 2 read literally is unsatisfiable within the bet's own declared non-goals — there will always be a value the reader cannot resolve, its seed drops, and the gate is green over code the engine did not check, which is exactly the test metrics.md :85-95 (PDR-0011) applied when it counted a fully-observable defect as a G2 false green. That is the single path from BET-AT-RISK to CANNOT-REACH. Criteria 1, 4, 5 and 6 do not move either way. Ratification requires all three artifacts spec :179 names — amendments to PRD :28 and :48-53, plus a dated metrics.md reading — and absent all three the spec treats the narrowing as not in force.
- **Should a builtin marker whose call SHAPE is malformed AND whose LEVEL value is unreadable emit the residual FACT, or does PY-WL-130's shape offence short-circuit before the reader is reached?**
  - What turns on it: Whether Task 12's matrix rewrite (condition 10) can be written at all, and whether spec :121's "every unreadable builtin LEVEL value takes it, with no exceptions" is literally true. Task 4 inserts `call_shape_offences` before the levels loop, so a shape-offending marker never reaches the reader; fifteen of the matrix's 21 rows are shape offences. Spec :190 requires every row to name a non-empty channel and assert that channel's finding is present, which cannot be discharged until the ordering is ruled. The census agent that found this filed it as a spec gap rather than a plan conflict and did not report it — surfaced here for that reason.
- **What is the `decorator_coverage` count's shape — a seventh sibling summary key, or a row-level attribution?**
  - What turns on it: Task 7 carries one of exactly two sanctioned golden re-freezes (plan :28), so the shape must be right in a single pass. Spec :168 says "counted in `decorator_coverage`, alongside `unknown_markers`" and names no key; spec :212 names only the existing `summary.unknown_markers`. The shipped golden's summary block is `additionalProperties: false`, so a wrong or deferred choice either consumes the re-freeze budget wrongly or leaves the key structurally rejected. A summary key is the reading condition 15 assumes; the owner or the spec should confirm it.

## Audit caveats

WHAT THIS AUDIT DID NOT COVER.

1. The Rust frontend source was never read. Nobody opened `src/wardline/rust/provider.py` or `analyzer.py` to check the shipped `_MARKER` pattern against spec §4.4's recogniser, refusal set and upper bound. Condition 11 therefore says "add a task implementing the spec" without any verification that the spec's account of the shipped pattern is accurate or that its widening is achievable as described. This is the largest unexamined surface in the audit, and it sits on the one criterion (6) the spec says is unmet regardless of the Python result.

2. Consumer repos (loomweave, warpline, legis) were never checked. Low risk — rev 6 alters no §13.1 contract and Tasks 17-21's engineering is unchanged — but the claim rests on reading the spec, not the consumers.

3. `diagnostics._BUILTIN_MARKER_IMPORTS` and spec :213's extension checklist were never checked for a rev-6 obligation. Rev 6 introduces no new marker import so there probably is none, but it was not verified in source; it is folded into sequence step 3.

4. `src/wardline/core/decorator_coverage.py` and `src/wardline/mcp/server.py` were not read directly; condition 15 rests on the plan's own statements plus the shipped `mcp_output_schemas.golden.json`, which was verified.

5. `src/wardline/scanner/analyzer.py` was not read in full, so "no plan task plumbs a census into `AnalysisContext`" rests on the plan's Files lists and a whole-plan grep (both of which returned zero) rather than on tracing every construction site.

CENSUS-SELF-REPORTED GAPS, surfaced as instructed.

6. Soundness condition 3 (the residual FACT is NOT suppressible, carrying §11.2's baseline posture) was flagged by one census slice as having no owner in its slice, and NO survivor picked it up. It is now condition 8, but note it was found by absence rather than by anyone checking the suppression machinery — no agent verified how a `Severity.NONE` FACT interacts with the shipped waiver and baseline paths.

7. The shape-gate/value-reader ordering (owner decision 3) was found by a census agent who explicitly declined to file it, judging it a spec gap rather than a plan conflict. It is promoted here because it blocks condition 10. Nobody analysed it further than that.

8. Two slices reported ambiguity about which task should own the census build (Task 2, 4 or 6) and reported the same gap from opposite sides. Condition 5 resolves it by creating a task rather than assigning it, which is a judgement, not a finding.

9. One slice noted it read Task 12 only to line 2700, so the "asymmetry pin" from :2702 onward is uncovered. Another read Tasks 7-21 only via grep.

10. Nobody verified that the residual FACT's emission produces a source line for every unreadable value shape; a `line_start is None` would today be invisible to `tests/grammar/test_output_determinism.py` because it filters to `Kind.DEFECT`, but would trip if that filter is ever widened.

ON THE KILLED CANDIDATES. I did not resurrect any of the six, and I judge all six kills correct. One leaves a residue worth naming without promoting: `crit1-artifact-unobliged-for-hole-1` was rightly killed as self-contradicting (it cited PRD :41-44 as its basis while asserting the artifact is obliged by nothing) and because `test_malformed_sibling_never_reduces_the_error_population` at plan :933-967 does pin that active ERROR defects fire on the false-green stack. The surviving residue is narrower: no committed test asserts the literal process exit code for hole 1 — the only exit-code checks in the whole plan are the repo self-scan at :4084 and the manual scratch repro at :4085, whose result lands in a Filigree comment rather than the repository. PRD-0003 criterion 1 requires each of the three repros to be "pinned by a committed regression test", and spec :188 obliges that artifact only for the two rev-6 repros, stating that no other spec text does. So hole 1's criterion-1 artifact is obliged by neither governing document. That is a plan-vs-PRD precision gap outside this audit's plan-vs-rev-6 frame; it is noted inside condition 22 as a suggestion rather than raised as a finding.

ON SEVERITY AND DEDUPLICATION. The `blocking` array is populated from post-refutation `confirmed_severity`, not from the candidates' original claims; six findings the skeptic downgraded (`stale-blob-pin-blocks-legal-start`, `goal-omits-rev6-deliverables`, `preamble-closed-fact-id-enumeration`, `coverage-map-has-no-owner-for-rev6`, `task2-step3-import-block-builds-the-one-sided-reader`, `no-builtin-custom-discriminator-on-the-value-reader`) appear as MAJOR conditions or as riders, not as blockers. The 76 survivors contain heavy duplication — the blob pin alone appears seven times and the Task 2 interface ten — and were deduplicated to 25 conditions by the edit required, not by the finding id. Where two members of one cluster received different severities from the skeptic, I took the more conservative reading and said so: the blob-pin cluster is MAJOR rather than BLOCKING because the check fails loud and safe and gates only Tasks 17/18/20 (plan :92, :101), building no wrong artifact.

INDEPENDENT VERIFICATION PERFORMED FOR THIS SYNTHESIS (no git command was run at any point, per the prohibition; Read, Grep and sed only). Spec §P3 form 5, §4.2, §4.2.1 in full including the case table and all five soundness conditions, §4.3, §4.4, §12 P9, §13.2's stage table. Plan lines 17, 21, 23, 27, 28, 36, 37, 42, 92-97, 101, 105-108, 380-393, 697-716, 970-972, 988, 1047-1051, 1601, 1841, 1931, 2596, 2657, 2674-2679, 4083-4085, 4092, 4096-4099. Whole-plan term counts confirming zero occurrences of "form 5", "form-5", "reference site", "UNREADABLE-MARKER-VALUE", "revision 6", "rev 6", "SeedContext", "rust/provider" and "rust/analyzer" (the four "census" hits are the unrelated `to_level` fixture census). Source: `src/wardline/scanner/rules/invalid_decorator_level.py:52-58` (the third `examples_clean` entry, verbatim), `tests/unit/scanner/rules/test_rule_examples_meta.py:66` (the `Kind.DEFECT` filter), `src/wardline/scanner/taint/provider.py:54-74` (`SeedResult`'s two fields and the builtin-exclusion docstring), `src/wardline/scanner/pipeline.py:30-36` (`_fp` is a positional NUL-join sha256, NOT spec :167's preimage). Product record: PDR-0017's Decision verbatim (scoped to `wardline-4928b75782`; "Hold S0 until the spec rev 6 amendment lands" explicitly rejected), PDR-0018's stated ordering "rev 6 → plan re-pin → preflight → execute", PRD-0003 :26-56 and :90-96 and :140-152, and metrics.md's latest G2 reading (2026-08-09, PDR-0012, 1 known hole) against PRD :30's baseline of 3.